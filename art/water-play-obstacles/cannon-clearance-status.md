# 水球动态镜头遮挡修订：待共用接入

2026-09-22，用户确认优先保留动态拉远拉近，解决不了遮挡才回退平稳相机。

候选改动将摄像机限制在课程两端以内、两侧距池沿至少 2m、水面上方至少 8m；横向位置收回后用高度补足取景距离，同时为完整核心椭圆留白。继续使用现有顶棚过滤，只改变本地水炮取景，不修改规则、网络、其他事件和主相机。

候选代码已在内存中接入实际相机类检查：1,560 个机位，水球／核心轮廓无场馆建筑遮挡及出框，8m 内前景采样从 15 次命中降至 0；3 项候选行为测试通过。独立浏览器 360 个状态通过，窄屏无横向溢出和外部请求。视线检查不覆盖人物、正式水体与动态对象；泳道绳不算建筑遮挡。

当前正式镜头已包含顶棚／横梁过滤，但尚未接入本候选的池岸机位约束。原制作阶段因 G 共用占用而暂缓；2026-09-22 分支收尾时将候选单独归档，不将旧占用状态当作当前阻塞。待接入内容保存在 [补丁](cannon-clearance-proposal.patch)，测试暂存为 `.pending`，不进入正式回归。

接续时先按 18.1 原子取得共用占用，重新读取最新镜头、辅助代码和文档，再局部应用补丁；保留当前其他事件的镜头修改。将 `.pending` 测试移至 `tests/cannon-camera-clearance.test.cjs` 并纳入既有水炮检查入口，运行实际测试及指定 TypeScript 检查，再运行 `audit_cannon_clearance.cjs` 生成实际接入版本的检查与预览，更新炮火说明、统一娱乐对应段及总计划。只释放自己的占用。未应用补丁时运行该审计脚本只能测到正式旧机位，不能据此复现候选通过结果。

分支收尾已验证 `git apply --check --ignore-space-change art/water-play-obstacles/cannon-clearance-proposal.patch` 可匹配当前代码；该选项兼容本机混合换行，不代表已应用。补丁本身包含正式测试文件的创建，应用整份补丁后不必再次移动 `.pending` 副本。

[当前候选离线预览](cannon-clearance-review.html)和 [采样结果](cannon-clearance-audit.json)为本机生成物，保留在本地，不随源码提交；新检出需在接入候选后按上述流程重建。尚未启动 Creator，未做引擎／真机验证，不能将候选离线结果记为正式接入完成。
