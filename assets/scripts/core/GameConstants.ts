export enum Rating {
    PERFECT = 'perfect',
    GOOD = 'good',
    BAD = 'bad',
}

export enum GameState {
    READY = 'ready',
    PRECOUNTDOWN = 'pre-countdown',
    COUNTDOWN = 'countdown',
    DIVING = 'diving',
    GLIDING = 'gliding',
    RACING = 'racing',
    FINISHED = 'finished',
}

export enum StrokeType {
    LEFT = 'left',
    RIGHT = 'right',
    BOTH = 'both',
}
