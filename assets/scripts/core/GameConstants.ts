export enum Rating {
    PERFECT = 'perfect',
    GOOD = 'good',
    MISS = 'miss',
}

export enum GameState {
    READY = 'ready',
    COUNTDOWN = 'countdown',
    DIVING = 'diving',
    GLIDING = 'gliding',
    RACING = 'racing',
    FINISHED = 'finished',
}

export enum StrokeType {
    LEFT = 'left',
    RIGHT = 'right',
    BOTH = 'both',
}
