# 海豚跳起跳点视觉加强

2026-09-13，按用户反馈加强玩家跟随镜头中起跳位置的反馈，正式接入真实离水事件。本次只替换起跳爆发，落水仍沿用原有逻辑。

- 起跳主水片一张，宽 2.0～2.3m、高 1.25～1.55m，寿命 0.60～0.72s，出生在身体起跳点的水面上方 0.22m。
- 两侧各 6 粒主滴和 3 粒细滴，朝两侧后方抬起；无连续补发。使用世界空间模拟，已发粒子留在起跳水面，不随角色升空。
- 复用左手波纹网格，在同一接触点播放 2.6 倍扩散波纹；常规手掌再次入水时恢复普通大小，重开和离屏清理。
- 新入口 `SwimmerRacePhases → CartoonSwimmerRig.triggerTakeoffSplash → SplashEmitter.triggerTakeoffSurfaceBurst`。保留 `DolphinJumpConfig.takeoffSplashScale` 的强度控制，新外观参数集中在 `SplashEmitterTuning.takeoffImpact`。
- 复用原有一套水片和两侧手部水滴系统、已有波纹网格、纹理和材质。单次主水片 1 张 + 水滴 18 粒；没有新系统或屏幕闪光。更大水片和波纹增加透明覆盖，需真机评估填充开销。
- 纯视觉变更，消费原有起跳阶段，不修改能量、飞行、计时或网络协议。精简 AI 配置保留其已有系统数量。

类型检查和 12 项水花回归通过，包括单张水片、两侧发射预算、固定水面位置、无连续补发与离屏不发射。独立浏览器用实际玩家比赛相机检查；微信真机可见度与帧耗仍待体验确认。
