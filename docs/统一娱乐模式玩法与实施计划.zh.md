# 统一娱乐模式玩法与实施计划

## 1. 文档定位

本文是六种娱乐事件整合为一个统一娱乐模式的总说明，也是后续实现、联机、性能和验收的唯一总计划。本文记录事件之间如何抽取、预告、轮换和共存；每个事件内部的模型、判定、表现和现有调参继续维护在对应玩法文档中：

- [心跳苏打大乱斗玩法说明](心跳苏打大乱斗玩法说明.zh.md)
- [鲨鱼大乱斗玩法说明](鲨鱼大乱斗玩法说明.zh.md)
- [漩涡冲浪赛玩法说明](漩涡冲浪赛玩法说明.zh.md)
- [炮火逃生赛玩法说明](炮火逃生赛玩法说明.zh.md)
- [定时炸弹模式玩法说明](定时炸弹模式玩法说明.zh.md)
- [水雷模式玩法说明](水雷模式玩法说明.zh.md)

截至 2026-09-19，六种独立玩法、每种事件十组多样化赛事广播、共用事件镜头、击倒急救、统一事件导演、200／400 米双赛程、按赛程分散触发和跨事件驻留均已接入。离线类型检查与自动回归需在每次调整后重跑；iOS／Android 微信真机的组合性能和双端弱网仍按本文阶段 E 验收，不用桌面结果替代。

## 2. 目标与边界

统一娱乐模式支持 200 米短局与 400 米长局。玩家只选择一个娱乐入口和比赛距离，比赛中由广播依次引入不同事件。旧事件产生的有限场景内容可以留下，让泳池逐渐变乱；但同一时间只允许一个事件处于主动爆发阶段，避免多个强事件一起抢占注意力。

本阶段不改变普通竞速、狂野模式、角色成长、划水输入和房间保活规则。六种旧模式 ID 继续保留，用于存档迁移、开发调试和单事件回归；正式入口只展示统一娱乐模式，显示名称确定为“娱乐模式”。

## 3. 每局事件组合

六个事件分成三类。每局先从每类各抽一个，再以 50% 概率从三个未选中的事件里等概率追加一个：

| 类别 | 候选事件 | 作用 |
| --- | --- | --- |
| 场地事件 | 漩涡、水雷 | 改造泳池路线，并留下长期障碍 |
| 争夺事件 | 心跳苏打、定时炸弹 | 促使玩家抢夺、靠近或互相转移风险 |
| 袭击事件 | 鲨鱼、炮火 | 制造一段集中躲避和强反馈 |

200 米每局有三个或四个不重复事件，仍按三类各抽一个、50% 概率追加一个的规则执行。400 米每局将六个事件打乱后抽取五个或六个，不重复；因为至多只缺一个事件，三类也始终都会出现。两种距离都禁止场地事件排在最后，确保残留物有发挥时间。玩家不会在开局看到完整事件表，只会通过广播得知下一个事件。

事件抽取必须使用由比赛种子派生的娱乐专属子种子，不能直接继续消费全局 `SharedRNG` 流，以免统一模式改变 AI、阵容或其他既有随机结果。

## 4. 统一生命周期与比赛节奏

每个事件使用同一套生命周期：

```text
等待 → 广播预告 → 爆发 → 必要结算 → 驻留 → 下一事件
```

- 比赛开始后先保留约 4 秒普通游泳时间。
- 三事件局提前 6 秒预告下一事件；四事件局提前 5 秒，仍保留足够阅读和反应时间。
- 广播结束后进入事件爆发阶段。
- 已发射的炮弹、主动追猎的鲨鱼和活动定时炸弹必须完成结算，才能进入下一段广播。
- 事件进入驻留后不再继续生成新波次，但允许已经产生的指定对象留在泳池。
- 当前事件完成必要结算后进入真实喘息段：200 米至少 5 秒，400 米至少 8 秒；达到最短时间后还要等领先选手抵达下一赛程锚点，才开始下一段完整预告。喘息期间不显示结束广播，既有驻留内容继续生效。
- 200 米三事件局的预告锚点约为全程 12%／48%／82%，四事件局约为 10%／35%／60%／85%；四事件局的爆发阶段继续按原时长约 75% 运行。
- 400 米五事件局的预告锚点约为 8%／29%／50%／71%／90%，六事件局约为 8%／24%／40%／56%／72%／90%；长局固定提前 6 秒预告，爆发时长继续按短局基准约 140% 运行。
- 主事件表在比赛仍未结束时完成后，只从炮火、鲨鱼、定时炸弹中选择返场事件；相邻两轮不重复，先留出确定性的 10～12 秒喘息，再播放完整预告。漩涡、水雷和心跳苏打不返场，避免突破驻留数量上限或重新生成一次性内容。
- 首位选手触壁完赛时，房主立即锁定导演：尚未激活的黄色预告直接关闭，等待锚点、喘息段和返场都不再安排新事件；如果已有事件处于爆发／必要结算阶段，则允许它自然结算后静默停止。漩涡、水雷、未拾取苏打等既有驻留内容继续保留到本局真正结束，不因首位选手冲线被强制清场。

事件导演以比赛模拟时间推进，不依赖渲染帧率，也不以某一位客户端的墙钟时间作为权威。广播是事件阶段的表现，不负责驱动玩法计时。

## 5. 六个事件的首版压缩参数

现有独立玩法均按完整 200 米设计，统一模式不能原样运行全部波次。第一版使用以下短事件参数：

| 事件 | 独立模式现状 | 统一模式首版 |
| --- | --- | --- |
| 心跳苏打 | 7 波，每波 3 瓶，共 21 瓶 | 2 波，每波 3 瓶，共 6 瓶；两波间隔约 4.5 秒 |
| 定时炸弹 | 6 轮，后续引信逐渐缩短 | 1 轮，约 8 秒引信，最后 0.8 秒锁定 |
| 炮火 | 10 次炮击 | 连续 3 次，间隔约 2.3～2.6 秒，保留 1.25 秒落点预警 |
| 鲨鱼 | 3 轮追猎 | 1 轮完整追猎，保留警告、入场和追击窗口 |
| 漩涡 | 四个泳段各 1 个 | 生成 1 个并驻留；性能和可读性验证后才考虑增加到 2 个 |
| 水雷 | 7 枚 | 生成 5 枚并驻留；每枚触爆后本局永久消失 |

这些数值是首版起点，不是永久定值。调参优先改变波次数量、事件间隔和驻留上限，不通过缩短必要反应时间制造难度。

### 5.1 400 米长局覆盖参数

400 米继续复用同一套事件控制器和美术资源，只扩大事件表与事件内部内容量，不复制第二套玩法实现：心跳苏打 4 波、定时炸弹 2 轮、炮火 5 次、鲨鱼 2 轮；漩涡仍最多 1 个，水雷仍固定 5 枚且每枚只触爆一次。长局可以同时抽到漩涡与水雷，但不会重复抽取同一种事件，因此驻留对象数量有硬上限。

以下原有小节描述 200 米短局参数；400 米仅按本节覆盖波次和轮数，其余命中、急救、AI、表现和联机规则完全复用。

### 5.2 心跳苏打

事件激活时出现第一波 3 瓶，约 4.5 秒后出现第二波 3 瓶。第二波出现后停止生成；未拾取药瓶仍可在后续比赛中争抢。已获得的体力、心率和转向失控按原规则自然结束。

### 5.3 定时炸弹

只发放一枚炸弹。炸弹爆炸、拆除或携带者冲线后，事件才算结算完成。炸弹不能永久驻留，也不能把已经完成比赛的选手重新拉回事件。

携带者被其他驻留危险击倒时，炸弹保留在原携带者身上，“急救中……”期间引信暂停，复活后继续倒数。急救或无敌中的选手不能成为初始携带者，也不能接收传递。携带者冲线则直接拆弹并结束本轮，不强制转移给落后选手。

### 5.4 炮火

黄色预告出现时，两门礼炮从观众席深处推到泳池长边看台中段，约 1.8 秒完成进场。礼炮的纵向位置与事件赛程锚点、炮弹落点彻底解耦：事件激活、每次开炮、返场和联机状态恢复都不得沿看台重定位；炮弹从固定炮口飞向不同落点。

连续完成 3 次炮击后停止开火，已经发射的炮弹必须落地并结算；事件结束后礼炮约用 1.2 秒退回观众席并隐藏。表现节点和共享网格继续保留供安全返场复用，但隐藏状态不再作为可见场景物驻留。访客晚加入或弱网恢复时，若权威状态已经进入炮击阶段，则直接在同一固定位置就位，不补播进场，也不新增联机字段。

### 5.5 鲨鱼

完成一轮主动追猎后进入低速巡游。巡游鲨鱼仍是会推挤或击倒选手的场景障碍，但不再主动锁定和发起下一轮追猎，并继续遵守急救与无敌保护。

### 5.6 漩涡

事件爆发时生成一个漩涡，完成展开后进入驻留状态。漩涡继续保留吸力、旋转水流、顺流收益和核心惩罚。

仅当本局事件表包含漩涡时，导演再以独立随机流进行一次 25% 的超级规格判定；200 米和 400 米都最多一个。超级漩涡优先放在激活点之后的下一处泳池物理中心，横向中心只在中线两侧 8% 内变化；若冲刺段已没有足够安全距离，则降级为普通漩涡，不贴终点强行生成。其范围、力度和美术规格与独立漩涡模式共用，六合一不维护第二套实现。

若同局还包含水雷，水雷在首次生成时就避开超级漩涡的泳池中心保留区；位置从未显示前便确定，不在玩家看见后瞬移。超级漩涡仍占用唯一的漩涡画中画，鲨鱼咬击和炮火等即时危险可按原有优先级覆盖它。

### 5.7 水雷

一次性投放 5 枚水雷，之后不补充新水雷。水雷继续漂浮并在碰撞时爆炸；每枚爆炸后本局永久移除，重新比赛才恢复。

水雷激活时必须检查全部在场选手的当前物理位置。若预设锚点落入选手周围的扩大出生安全椭圆，该雷先保持隐藏且不参与碰撞；房主确认安全区连续清空后才启用。出生保护的启用掩码随 `S|` 快照同步，访客、晚加入和房主迁移不得各自决定是否启爆。该规则只处理动态投放，不改变独立水雷模式开赛前已经可见的正常布局。

赛道生成物统一遵守“不能出现即结算”：危险障碍使用出生避让或预警／启用阶段；奖励拾取物生成在当前领先位置前方，并禁止同一刷新帧直接发放。现有六合一心跳苏打首波位于激活锚点前方 6 米（400 米为 5 米），垃圾先在领先位置前方 7 米经过 1.35 秒落水过程，炮火沿用落点预警；这些玩法不再额外套用水雷的伤害安全半径。

## 6. 广播视角与提示规则

预告采用泳池广播站的搞笑口吻，不使用严肃的“下一事件：炮火来袭”模板。黄色预告负责气氛和提前告知，左上角统一显示“广播通知”；事件真正激活后，原位置切换为红色行动提示，并把左上角改为该事件的具体栏目，告诉玩家现在要做什么。

六种事件各维护十组广播，每组由一条黄色搞笑预告和一条红色行动提示组成；普通漩涡与超级漩涡分别维护十组，避免强化事件仍反复使用单句。黄色文案可以自由发挥，红色文案可以变化趣味前缀，但必须保留稳定、明确的操作信息。单条预告最多 31 个字符，行动提示最多 24 个字符，继续使用单行 `SHRINK` 兜底，不能通过增加第二行挤占个人反馈区域。

文案编号由比赛种子、事件类型、是否超级规格和单调激活序号派生，不直接消费全局玩法随机流。预告阶段为即将到来的激活序号选定一组，事件激活后的红字沿用同一组编号，展示过程中不再换句；十组使用与十互质的轮换步长，同一事件前十次取样不重复。联机各端复用已经同步的比赛种子和导演激活序号，因此不新增网络字段；断线恢复、迟到快照或房主迁移后仍能重建同一句广播。

### 6.1 广播栏目、正文与图标规范

每条赛事广播固定拆成三个职责互不重复的信息区：左上角栏目回答“这是什么消息”，正文回答“发生了什么／玩家该做什么”，左侧图标回答“属于哪个事件”。正文不得再增加“泳池广播：”等与“广播通知”重复的前缀，也不得把栏目名称拼入正文。栏目优先使用四个汉字并从受控词库中选择，不根据单条文案临时造词，以保证色块宽度、字重和识别速度稳定。

| 栏目 | 使用阶段 | 默认色调 | 对应图标 |
| --- | --- | --- | --- |
| 广播通知 | 六种导演搞笑预告，以及无法归入具体事件的通用通知 | 黄色预告 | 当前事件图标；无事件时用广播图标 |
| 补给投放 | 心跳苏打激活、后续补给波次 | 黄色或红色 | 苏打 |
| 炸弹接力 | 定时炸弹激活、装雷与传递阶段 | 红色危险 | 定时炸弹 |
| 漩涡警报 | 普通／超级漩涡激活和逼近提示 | 黄色或红色 | 漩涡 |
| 水雷警报 | 水雷投放与危险启用 | 黄色或红色 | 水雷 |
| 鲨鱼警报 | 鲨鱼现身、锁定和追击 | 黄色或红色 | 鲨鱼 |
| 炮火警报 | 炮火激活、瞄准和落点预警 | 黄色或红色 | 礼炮 |
| 赛道异物 | 垃圾等不属于六种主事件图标的赛道障碍 | 黄色预警 | 广播；后续有专属图标再替换 |
| 咬伤播报 | 某位选手被鲨鱼咬伤的结果 | 红色危险 | 鲨鱼 |
| 炮击命中 | 某位选手被炮弹核心命中的结果 | 红色危险 | 礼炮 |
| 炸弹爆炸 | 定时炸弹爆炸并击倒选手的结果 | 红色危险 | 定时炸弹 |
| 拆弹成功 | 带雷冲线或其他权威拆弹结果 | 绿色成功 | 定时炸弹 |
| 警报解除 | 鲨鱼退回巡游、危险阶段明确结束 | 绿色成功 | 对应事件图标 |

同一事件允许随阶段更换栏目。例如鲨鱼现身与追击使用“鲨鱼警报”，真正咬中选手后使用“咬伤播报”，追击结束后使用“警报解除”；炮火预警使用“炮火警报”，命中结果使用“炮击命中”。栏目描述消息语义和阶段，不等同于玩法名称。

栏目、正文、图标和色调只属于本地表现层，必须由既有权威事件与同步状态确定，不新增广播字符串或栏目字段到联机协议。排队显示的消息必须连同栏目、图标、色调和时长一起入队，禁止下一条消息沿用上一条栏目。新增娱乐事件时，必须在本节词库中复用或补充栏目，并同时明确预告、激活、命中／成功、解除四类阶段是否存在；缺少栏目或图标映射的事件不得默认写成“娱乐突发”。无法精确分类时才回退为“广播通知”。

示例：

| 事件 | 广播主句 | 行动提示 |
| --- | --- | --- |
| 炮火 | 岸边工作人员已经完成装填，并拒绝说明用途。 | 6 秒后开火 · 注意水面落点 |
| 鲨鱼 | 救生员表示这条鱼应该已经吃过饭了。 | 6 秒后鲨鱼入场 · 当心锁定 |
| 定时炸弹 | 广播站友情提醒：请勿接收陌生人的随身物品。 | 6 秒后随机发放定时炸弹 |
| 水雷 | 泳池清洁工作临时取消，原因不便透露。 | 6 秒后水雷入场 |
| 漩涡 | 水循环系统进入了它自己的创作阶段。 | 6 秒后形成漩涡 · 远离核心 |
| 心跳苏打 | 检测到不明饮料入池，保质期问题暂不讨论。 | 6 秒后苏打出现 · 抢到即可恢复体力 |

最后 1 秒可以切换警告颜色或加快已有指示灯，不制作高开销的全屏程序化特效。广播和个人反馈复用现有 `EntertainmentEventBanner`；事件画面复用唯一一套 `RaceEventPictureInPictureCamera`。定时炸弹首次装雷短显 1.2 秒，传递短显 0.8 秒并带 1 秒重开冷却，最后 0.8 秒锁定强制显示，爆炸或拆弹后保留约 1 秒；本人持雷也使用同一镜头和第一人称状态。画中画不替代主画面的落点、锁定、挂载炸弹和世界障碍提示。

赛事广播采用低矮斜切的深色运动条：四字栏目使用黑色文字，固定居中放入顶部左侧的黄色／红色／绿色状态色块；苏打、炸弹、漩涡、炮火、水雷或鲨鱼图标固定锚定横幅左边缘并统一使用水雷截图位置。栏目和图标都不随正文长短或画中画收窄漂移。具体广播独占深蓝主横条，在避开图标和右侧斜纹的固定内容框内水平居中并以 `SHRINK` 兜底。预告、行动、危险和成功状态只替换同一底板的色调与栏目，不新增第二行。心跳苏打拾取使用下方独立的橙红能量卡，固定标题和心电装饰烘焙在底图中，体力恢复量与当前心率由运行时 Label 分区显示。两套节点均在 HUD 绑定时一次创建，只在状态边沿切换贴图、栏目、正文、显隐和短 Tween；`update()` 只处理到期关闭，不逐帧重写 Label 或重绘 Graphics。

共享镜头冲突按“鲨鱼危险镜头 > 炮火落点 > 定时炸弹锁定与结算 > 漩涡教学俯视”处理。鲨鱼镜头占用期间，炮火只缓存最新落点，不逐帧切换相机模式；被高优先级事件占用时，定时炸弹继续缓存权威携带者和剩余引信，镜头空闲后只恢复仍有意义的锁定或结算阶段，不补播已经过时的普通传递特写。

事件激活时恢复单行广播布局：黄色预告被同一组编号的红色行动提示替换，红字完整保留约 3 秒后关闭。文案只在导演进入预告或激活的状态边沿计算和写入，不进入比赛逐帧更新。导演预告和激活文案在自己的展示期内具有最高广播优先级，子玩法的波次、命中和退场短提示不得覆盖；个人命中、拾取和传递反馈继续使用独立的个人反馈行。不要在黄色主句下再叠第二行，避免向下占用个人反馈／Toast 区域。

所有提示必须有明确的关闭路径。炮火、定时炸弹和重生无敌提示复用同一套无图标斜切状态条美术，说明文字与右侧关键值均由程序 `Label` 提供；关键值可以显示秒数、发数、轮数或简短动作词。三类提示各自持有独立节点和生命周期，不附着于赛事广播，也不会随广播消失或互相接管。炮火和定时炸弹的持续状态条只在对应事件真正爆发时显示；事件结束后立即关闭状态条，六合一不再播报“本轮结束／下一事件准备中”，喘息段保持安静，到达下一锚点后才显示黄色预告。独立玩法仍可保留“向终点冲刺／全力冲刺”和鲨鱼退场等完整赛制反馈。场景对象允许驻留不代表 HUD 或提示文字继续驻留，联机重复快照也不得重新播放已经消费过的结束提示。

## 7. 驻留规则

采用“看得见，就仍然有效”的一致性原则：

| 内容 | 是否驻留 | 驻留语义 |
| --- | --- | --- |
| 漩涡 | 是 | 保持可见和水流判定 |
| 水雷 | 是 | 未触爆的水雷继续漂浮；已触爆的永久移除 |
| 未拾取心跳苏打 | 是 | 后续仍可拾取，不再生成新波次 |
| 鲨鱼 | 是 | 低速巡游，不再主动锁定 |
| 岸边礼炮 | 否，节点池保留 | 事件结束后退场隐藏；返场复用原节点并从同一固定位置进场 |
| 定时炸弹 | 否 | 必须爆炸或拆除后才能结束事件 |
| 炮弹、爆炸、水柱、警告圈、广播画中画 | 否 | 按各自短生命周期回收 |

漩涡、水雷和未拾取苏打固定在真实泳池物理位置。玩家折返后再次经过同一处，仍然会遇到它们；不能只按某一个 50 米赛段显示，也不能出现可见但无判定或有判定但不可见的状态。

比赛结束、退出、断线退赛和保活重开时，导演及全部驻留状态必须一次性清空。比赛状态离开 `RACING` 的同一状态边沿还要立即重置急救控制器与遮罩、无敌表现、炮火／炸弹状态条、鲨鱼锁定标记和事件画中画，不能等到颁奖页或下一局再清。重新比赛使用新种子和新事件组合，不调用 `endGame`。

## 8. AI 与多事件共存

现有事件分别给 AI 写入横向目标，统一模式必须增加一个意图仲裁层，不能依赖多个控制器的覆盖顺序。建议优先级：

1. 即将命中的炮火核心、鲨鱼锁定和近距离水雷。
2. 定时炸弹携带、传递和躲避。
3. 漩涡绕行或借力路线。
4. 心跳苏打争抢。
5. 普通比赛路线和身体避让。

当前爆发事件可以获得小幅优先加成，但迫近的驻留危险始终优先。仲裁结果保持一个短暂迟滞窗口，避免 AI 在两个目标之间逐帧左右摇摆。远程真人副本不参与本地 AI 决策。

## 9. 击倒、急救与连锁保护

- 鲨鱼、炮火核心、定时炸弹和水雷直接命中统一使用急救重生规则。
- 四类致命命中统一触发大水花与短促翻起的受击姿态；整个 3.5 秒急救阶段，角色保持专用的侧翻漂浮眩晕姿态，头顶显示三颗沿同一椭圆轨道错相环绕的卡通星星，让旁观者能直接看出其正在急救。三颗星复用同一张单星贴图，各自保持正向，并通过轨道位置、远近缩放和透明度表现前后穿行，不再旋转一张烘焙好的三星环图片。每颗星后方复用现有渐隐光带拼成两段短弧拖尾，拖尾沿刚刚经过的椭圆轨迹排列，前侧更亮、后侧更淡，既交代运动方向又不形成一整张平面圆环。星星使用独立的屏幕空间节点，以选手游泳根节点为稳定锚点放在人物正上方，不继承头部骨骼的翻转、俯仰或入水位置；轨道和拖尾以 20Hz 更新，只消费现有权威急救状态，不新增联机字段。重生时姿态、星星和拖尾一起收起并恢复正常游姿。
- 炮火、定时炸弹和水雷统一采用“直接命中者击倒、外围范围内只击退”的两层爆炸语义；独立入口与六合一不改变同一事件的命中结果。
- 击倒后的急救等待统一为 3.5 秒；命中后的前 0.45 秒先完整展示爆炸水花、人物翻倒和头顶星星，随后背景渐暗，运动转播式急救牌从左侧高速切入、轻微冲过目标后回弹接管画面。卡片沿用娱乐广播的深蓝斜切主体、青色底沿和红橙状态签，左侧使用非拟人的救生圈医疗徽标，右侧保留三段状态灯；底板与“紧急救援”“急救中”运行时文字分离。“急救中”仍以一个、两个、三个点循环，底部救援进度轨按权威剩余时间以 10Hz、百分级步进推进；入场结束后卡片保持稳定，不做整卡循环浮动或呼吸缩放。权威急救结束时必须立即释放输入阻挡，急救卡可继续向右上方短促退场，顶部无敌保护条同步弹入；视觉退场不得延迟零速入水和控制权恢复，2 秒无敌保护不变。进度轨只缩放一次创建的静态几何，不逐帧 `Graphics.clear()`，也不增加联机字段。
- “急救中……”和重生无敌阶段不能再次受到致命击倒。
- 漩涡可以继续施加普通水流，但不能在保护阶段触发新的击倒。
- 复活位置使用权威赛程距离和自己的泳道中心，初速度为零。
- 复活点如果处于水雷或漩涡危险核心内，保护阶段必须足够让玩家离开；不得通过本地随机选择不同复活点。
- 已完成比赛的选手不再成为炸弹、鲨鱼或炮火目标，也不再触发场景危险结果。

## 10. 联机设计

### 10.1 权威与随机

- 房主选择本局事件组合、事件顺序、事件阶段和所有影响结果的随机目标。
- 事件表使用比赛种子派生的独立子种子。纯视觉水花和装饰抖动可以继续使用 `Math.random()`。
- 真人位置仍由本人权威，AI、事件结果、名次和驻留状态由房主权威。
- 客机可以预测倒计时和视觉运动，但不能自行决定拾取、爆炸、咬合、击倒或事件切换。

### 10.2 导演快照

统一模式需要在周期 `S|` 快照中追加紧凑状态，不能只依赖一次广播：

- 比赛／导演实例编号。
- 三至六个事件 ID、数量和顺序；200 米为三至四个，400 米为五至六个。
- 当前事件序号、生命周期阶段和修订号。
- 当前激活序号、最后激活事件、返场轮次和返场事件；同一种事件再次出现时不能只靠已激活位掩码判断，客机错过整段返场后也要能静默重置旧子状态。
- 预告、爆发或结算剩余时间。
- 事件专属子种子或可复现的静态布局标识。
- 心跳苏打已拾取掩码。
- 水雷存活掩码和统一漂浮时间基准；已爆水雷不能被快照复活。
- 漩涡物理锚点、旋向、激活状态和导演特殊事件掩码。
- 定时炸弹携带者、引信、锁定和暂停状态。
- 鲨鱼与炮火沿用并扩展现有权威状态。
- 各泳道急救／无敌阶段继续沿用现有恢复快照。

静态布局由种子和事件实例推导，不逐帧发送每个模型的完整浮点坐标。会变化的内容优先使用位掩码、量化整数和递增修订号。

### 10.3 传输与丢包恢复

- 需要及时且可靠的命中、拾取、传递和结算事件继续进入帧事件通道。
- iOS 或异常网络下帧通道可能不可用，现有广播输入兜底必须继续承载同一事件格式和同一单调序号。
- `broadcastInRoom` 是尽力而为；事件开始消息即使丢失，下一份周期快照也必须恢复正确阶段。
- 即时事件和快照可能以任意顺序抵达，所有消费者必须以实例编号和修订号去重，不能重复播放击倒或重复结算。
- 开赛原子数据应同时包含比赛距离和比赛种子；事件表由同种子和距离确定。周期快照再次携带事件数量、事件表和六个固定锚点槽位，支持恢复和房主迁移。

### 10.4 房主迁移与保活重开

新房主从最近快照继续当前事件和全部驻留状态，不重新抽取、不重置引信、不复活已拾取道具。保活重开继续复用现有会话，但必须生成新的比赛实例编号、种子和事件表，并清空上一局驻留状态。禁止为重新比赛调用 `endGame`。

定时炸弹终点收尾修复接入后 `NET_RACE_PROTOCOL_VERSION` 已提升到 82；水雷快照使用原保留槽同步启用掩码，导演快照尾部继续携带激活序号、最后事件、返场轮次和返场事件，房间规则和开赛消息继续同步 200／400 米距离。定时炸弹的初始目标筛选、人物真实接触消费、贴身扫掠、定向短传确认及首位完赛后的待触发轮次取消只由房主运行，仍复用既有传递事件与完成掩码快照；收尾锁定复用导演阶段字段，由房主触发并随周期快照同步，客机和迁移后的新房主不得自行按本地完赛时刻另算。同一修订内，导演和炮火的剩余时间只能减少；水雷快照按权威漂移时钟拒绝倒序包，不能复活已爆障碍。旧客户端不能与当前定时炸弹终点收尾、真实碰撞交接、3.5 秒急救、触雷扫掠、出生安全、巡游鲨鱼补咬、事件节奏、超级漩涡、水雷存活及全局急救语义混跑。

## 11. 性能预算

第一版同时存在上限：

| 内容 | 上限 |
| --- | ---: |
| 驻留漩涡 | 1 个 |
| 活动水雷 | 5 枚 |
| 未拾取苏打 | 200 米最多 6 瓶；400 米最多 12 瓶及其既有提示光柱 |
| 巡游鲨鱼 | 1 条 |
| 池化礼炮 | 2 门，仅炮火预告与爆发阶段可见 |
| 活动定时炸弹 | 1 枚，仅爆发／结算阶段 |
| 事件画中画 | 1 个 256×144 RenderTexture |

性能重点不是低面数模型数量，而是水面、漩涡、苏打光柱、水花和画中画同时出现时的透明填充。必须遵守：

- 事件控制器与表现对象每局建立一次，轮换时重置和显隐，不反复创建、销毁。
- 普通与超级漩涡的六份低面数网格和共享材质在倒计时前一次性预热；赛中只挂载固定三层表现，不增加运行时贴图、粒子、`Graphics` 或着色器种类，单个漩涡的可见绘制层数不变。
- 水雷、爆炸、水柱、落点和警告圈使用固定容量对象池。
- 未激活控制器立即返回，不扫描泳者、不格式化文字、不写节点状态。
- 炮火波次耗尽后先检查固定波次位图再扫描泳者；全部水雷失活后停止漂移、出生保护和泳道碰撞扫描。
- 玩法和网络状态按模拟频率运行；水雷漂浮、远处装饰和世界提示可降至 20～30Hz。
- 画中画最高 30Hz，只在预告、定时炸弹短特写／锁定或必要命中演出期间启用；隐藏时相机必须完全停渲染。定时炸弹普通引信阶段不得常驻画中画，低性能档允许降至 20Hz。
- 赛中 Label 最多按可见精度和 10Hz 更新，最终字符串相同则不赋值。
- 普通 HUD 不逐帧 `Graphics.clear()`；全屏急救遮罩只在尺寸变化时重建。
- 比赛热路径不创建临时数组、对象、闭包、`Color`、`Vec2/3/4` 或格式化调试字符串。
- 远离镜头的驻留物可以停止装饰动画，但房主权威碰撞和水流判定不能停止。

### 11.1 独立玩法与六合一复用约定

六个独立玩法是正式的规则、美术和手感调试入口，不是将来会被六合一替换的临时版本。六合一负责抽选、预告、轮换、驻留和组合性能管理，不复制第二套玩法实现。

- 模型、Prefab、网格、贴图、材质、动画、爆炸、水花、提示组件和表现控制代码都只维护一份。独立玩法与六合一必须引用同一资源路径和同一表现模块，不得建立 `Unified` 版本的复制资源或分叉类。
- 两种入口复用代码和资源，但每局创建各自的状态实例。独立玩法中的拾取、倒计时、命中、重生和联机 revision 不得与六合一共享运行时对象或残留状态。
- 在独立玩法里完成的模型、材质、颜色、形状、基础尺寸、动画和触发时机调整，视为公共美术基线；六合一应自动获得这些改动，不要求重新制作或重新调完整套表现。
- 参数分为三层：公共美术参数、独立玩法完整赛程参数、六合一组合覆盖参数。公共美术参数只有一份；独立玩法保留完整波次和持续时间；六合一只覆盖事件数量、事件时长、驻留上限、远处亮度、装饰动画采样频率、低性能档可见距离和画中画优先级等组合语义。
- 如果一次调整只应影响独立玩法，必须写入独立玩法预设，不得在共享控制器或共享美术模块中加入隐蔽的模式判断。如果属于模型、材质、动画、触发或公共性能修复，则默认同时影响独立玩法和六合一。
- 六合一接入后的二次调试属于组合验收，不是重做美术。只检查多事件同时存在时的视觉主次、广播与画中画冲突、驻留衔接、透明叠加、对象上限、帧耗和网络恢复；需要收敛时优先调整六合一组合覆盖参数，不回写破坏已经确认的独立玩法表现。
- 单玩法专属的美术参数和验证记录继续更新对应玩法总文档；跨玩法的驻留上限、组合降级和覆盖参数更新本文。每次修改单玩法后先在独立入口验收，再用六合一组合场景做一次回归即可。

验收时必须能证明：修改任一单玩法的公共美术实现后，六合一无需复制资源或重复接入即可显示新版；切回独立玩法时，六合一的压缩波次、降频和驻留上限不会反向污染完整模式。

### 11.2 娱乐道具水花分级实现与验收

截至 2026-09-19，跨玩法共用的三档水花已经完成代码接入；本节同时记录当前实现、美术基线、组合预算与待完成的真机验收。各单玩法总文档只记录自身档位和参数，不复制一套 `Unified` 资源或实现。

#### 11.2.1 当前基线与改造目标

当前 `EntertainmentWaterSplash.ts` 统一维护轻落水、重入水和水爆三份低模几何、一份无光照顶点色透明材质及固定对象池。苏打与软垃圾使用轻落水；硬垃圾、水雷水下扰动／破水和鲨鱼首次上浮使用重入水；炮弹、水雷与定时炸弹使用水爆。固定网格、共享材质、对象池和 20Hz 变换更新继续保留，小物件不再表现成带暖色爆心的缩小爆炸。

精修目标按以下优先级执行：

1. 先让轻物落水、重物破水和爆炸具有不同轮廓、方向与节奏，再考虑增加粒子数量。
2. 波纹负责确认水面接触和冲击强弱，不把规则圆环做成精确碰撞范围；外缘应有断口、宽度变化或不均匀明暗，避免误读为预警区。
3. 同一接触只产生一套主水面波纹；世界水爆与人物受击水花分工，不在同一点叠两套大环和两套白色水冠。
4. 保持纯视觉改造，不改变拾取、命中、击倒、速度、AI、比赛结果或网络协议。
5. 不把道具冲击源加入全屏水面着色器，不新增水波 RenderTexture、运行时 `Graphics` 或逐帧重建网格。

#### 11.2.2 三档公共美术基线

| 档位 | 主要用途 | 轮廓与组成 | 波纹 | 建议可见期 |
| --- | --- | --- | --- | ---: |
| 轻落水 `LIGHT_ENTRY` | 心跳苏打、软垃圾、小型漂浮物落水 | 低矮不对称水片，0～4 颗较大水滴；不使用暖色爆心和九束放射水柱 | 一圈细、短、带断口的波纹；后半程快速淡出 | 0.32～0.45 秒 |
| 重入水 `HEAVY_ENTRY` | 硬垃圾、水雷破水上浮、鲨鱼首次上浮 | 明确的主方向水片或水冠，4～8 颗水滴；整体比爆炸低、比轻落水更有重量 | 一圈主波纹；必要时一圈很淡的残缺辅纹，不出现暖色爆心 | 0.45～0.65 秒 |
| 水爆 `EXPLOSION` | 炮弹、水雷爆炸、定时炸弹结算 | 保留暖色爆心、环形水冠、立体弧形水柱和少量块状水滴；不同爆炸只调规模、方向偏置和节奏，不复制网格 | 两圈错时扩散的水面冲击环，外圈弱于内圈 | 0.52～0.65 秒 |

档位只定义公共美术语义，不直接等同于玩法伤害。水花半径不得暗示一个与实际核心／外围判定不一致的精确边界；需要表达炮火预警或爆炸范围时，继续使用现有规则标记。

方向规则如下：

- 苏打和垃圾继承空中落水前最后一段水平运动方向，主水片略向运动方向前方掀起，不能只靠随机旋转。
- 水雷破水沿上浮方向形成偏低的环形水冠；水下扰动阶段只显示低矮波纹和局部翻涌，不提前出现完整水柱。
- 鲨鱼首次上浮的水片沿鲨鱼前进方向向两侧分开，保留身体冲开水面的重量感，不使用爆炸暖色。
- 炮弹、障碍水雷和定时炸弹以径向爆发为主，可根据炮弹来向或人物被击飞方向加入轻微不对称，但不能把水柱整体吹到一侧而失去爆点。
- 纯视觉碎散角度可由事件编号派生或使用 `Math.random()`；不得进入玩法随机流，也不得影响碰撞和击退。

#### 11.2.3 各入口映射与重叠规则

| 入口 | 目标档位 | 水面效果所有者 | 特殊规则 |
| --- | --- | --- | --- |
| 心跳苏打落水 | `LIGHT_ENTRY` | 道具表现池 | 三瓶错峰落水可以同时可见；不得互相抢占仍在播放的槽位 |
| 软垃圾落水 | `LIGHT_ENTRY` | 道具表现池 | 轮廓更宽、更低，强调轻飘拍水 |
| 硬垃圾落水 | `HEAVY_ENTRY` 的精简规格 | 道具表现池 | 水片略高但不出现爆心，和软垃圾一眼可区分 |
| 水雷水下扰动 | `HEAVY_ENTRY` 的仅波纹子形态 | 水雷表现池 | 只启用共享环带节点，不出现水柱和暖色；随后破水阶段再播放完整重入水 |
| 水雷破水上浮 | `HEAVY_ENTRY` | 水雷表现池 | 延续现有水下扰动→破水→稳定漂浮顺序 |
| 鲨鱼首次上浮 | `HEAVY_ENTRY` | 鲨鱼入场表现 | 只在首轮入场播放一次；后续巡游和再次锁定不重复 |
| 炮弹、水雷、定时炸弹爆炸 | `EXPLOSION` | 世界水爆表现池 | 世界水爆负责爆心、水冠和带高度的双层断续波峰；水柱先结束，水面余波继续扩散沉降 |
| 炮火／水雷／定时炸弹直接命中人物 | 现有角色大水花叠加层 | 角色 `SplashEmitter` | 角色层只负责随身体翻起的竖向水片和水滴，不再生成第二套大水面环 |
| 鲨鱼咬伤人物 | 现有角色定点大水花 | 角色 `SplashEmitter` | 因没有额外世界爆炸，角色层保留一圈强波纹，并固定在保存的咬合接触点 |
| 爆炸外围冲击人物 | 现有较弱角色水花 | 角色 `SplashEmitter` | 不再为每名外围受击者追加世界水爆或冲击环 |
| 玩家之间的中强身体碰撞 | 通用 `BODY_CONTACT` 碰撞水花 | 独立碰撞表现池 | 方向水花加一圈小型残缺椭圆波纹；娱乐模式只提高通用预设倍率，不复用水爆档、不出现暖色爆心或双层冲击环 |

多人同时受击时仍保留统一致命击倒的大水花语义，但背景 AI 沿用现有精简粒子 LOD；不能为了水花分级把全部 AI 自动扩成玩家同规格。事件画中画正在观察的目标继续使用既有水花裁剪例外，不能被主镜头视锥误关。

玩家身体碰撞水花不是第七种娱乐事件，也不纳入本节三档娱乐道具水花池。它是标准、狂野和娱乐比赛共用的身体接触反馈：轻擦与持续贴靠不触发，标准模式只显示强碰撞，狂野模式使用基线，娱乐模式在同一实现上提高约 20%～30% 的视觉强度。详细阈值标定、空间方向、4 槽专用池和联机策略统一维护在 [碰撞柔性反馈试用版](碰撞柔性反馈试用.zh.md)。

#### 11.2.4 共享实现与对象池

当前通用水花几何、阶段采样和固定池槽已经集中在 `assets/scripts/core/EntertainmentWaterSplash.ts`；各玩法表现层只提交档位、世界接触点、方向、强度、所属玩法和是否只显示波纹，不再自行复制水花网格与材质。

- 每局按三档预热五份共享网格：轻落水单网格、重入水本体／单层波峰、爆炸本体／双层波峰，共用一份无光照顶点色透明材质；不为每个玩法创建不同材质实例。重入水不复用更高面数的爆炸双层波峰，保持约 256 三角形预算。
- 轻落水保持单个 `MeshRenderer`；重入水和水爆最多拆为“水片／水柱”和“水面环”两个固定渲染节点，使水冠先起、波纹稍后扩散。隐藏节点不参与更新。
- 水爆总可见期约 0.95 秒，其中水柱在约 0.6 秒结束，独立水面环以白蓝高亮浪尖、低矮立体坡面和透明外裙继续扩散并逐渐压回水面；不增加第三个透明节点、材质种类或逐帧透明度写入。
- 当前固定池容量为轻落水 4 槽、重入水 3 槽、水爆 3 槽。池满时复用剩余寿命最短的槽，比赛中不扩容或临时创建节点。
- 已发水花固定在出生世界坐标，不跟随道具、鲨鱼或人物继续移动；接触点统一校准到 `waterY` 上方约 2～5 厘米，具体高度以正式比赛镜头消除深度闪烁为准。
- 共享池以活跃计数做零成本空闲门控；没有水花时不累计计时也不扫描槽位。有水花时以 20Hz 用索引循环检查 10 个预建槽，纯波纹子形态跳过隐藏水柱的变换写入。触发、重置和显隐只在状态边沿写入，每槽复用标量和向量，热路径不创建数组、对象、闭包、`Color` 或 `Vec3`。
- 定时炸弹爆炸的世界水花和结算画中画共同使用可靠结算事件中的权威赛程距离和横向爆心换算，各端不再各自读取可能有预测差异的泳者节点；结算镜头复用现有 256×144 RenderTexture，以较高俯角容纳被炸角色和完整冲击波，不新增相机或网络字段。快照补漏缺少结算坐标时保留本地节点回退，确保断线恢复仍有表现。
- 重开、退出、玩法隐藏和控制器销毁必须清空全部剩余寿命并关闭节点；重复调用清理保持幂等，不让上一局水花在下一局补播。
- 周期快照只恢复道具和事件的当前状态，不补播已经过去的一次性水花。可靠命中事件继续按既有 revision 去重；晚加入看见已落水道具时直接进入漂浮状态。

首版优先继续使用低模几何和顶点色，不新增贴图。只有正式比赛镜头证明低模细环产生明显锯齿且无法用几何解决时，才评估一张共用小尺寸波纹纹理；即使采用纹理，也必须使用环带网格裁掉透明中心和四角，不能用大透明方片覆盖水面。

#### 11.2.5 性能硬预算与降级顺序

- 轻落水单槽不超过约 128 三角形；重入水不超过约 256 三角形；水爆的水柱与波纹合计不超过约 480 三角形。顶点量不是主要风险，但不得用高分段圆环换取肉眼不可见的圆滑。
- 相对当前一个水爆一个渲染节点的基线，三个水爆同时可见时最多增加 3 次绘制，用于独立错时波纹；轻落水不得因为精修增加第二次绘制。
- 波纹使用真实环带或残缺环带几何，不使用整张透明圆盘或全屏四边形。尺寸调大前先检查透明覆盖面积，不能只看三角形数量。
- 不修改 `RagingPoolWater.effect` 的全屏折射路径，不增加道具扰动数组、每像素事件循环或新的 RenderTexture。现有八泳者局部扰动仍只在既定镜头条件下启用。
- 低性能档首先关闭重入水的淡辅纹和水爆细小水滴，其次缩短远处水花可见期；主接触水片和第一圈波纹必须保留，玩法与联机状态完全不变。
- 玩家碰撞水花已使用独立 4 槽池，每槽一个约 84 三角形的合并低模网格和一个渲染节点，最多增加 4 次透明绘制；不抢占轻落水、重入水或水爆的 10 个既有槽。最坏组合验收必须把八人碰撞拥挤加入下述事件叠加场景。
- 同机、同构建、同机位与当前版本做 A／B：效果窗口的 1% low 帧率建议下降不超过 5%，峰值帧耗建议增长不超过 10%；超过任一门槛时按“辅纹→细滴→远处寿命”的顺序收敛，不先降低玩法模拟和网络频率。
- 反复触发至少 20 轮后，节点、组件、材质和网格数量保持稳定；预热后不得出现随次数增长的内存、监听或运行时分配。

最坏组合真机用例至少包括：三瓶苏打错峰落水并保留光柱；三枚水雷近时爆炸；炮火命中并同时掀翻多名泳者；鲨鱼上浮或咬伤期间打开事件画中画；漩涡、水雷、苏打光柱、角色水花和画中画同时可见。iOS／Android 微信体验版都要检查透明排序、瞬时掉帧、持续发热和低性能档降级，桌面预览不能替代。

#### 11.2.6 实施状态与验收

1. **已完成公共骨架**：三档配置、共享低模几何、一份材质和固定池已集中实现；独立玩法与六合一通过 `GameManager` 持有的同一实例进入。
2. **已完成轻落水**：苏打和软垃圾使用轻落水，硬垃圾按重量提升到重入水；轻落水没有暖色爆心，且只占一个渲染节点。
3. **已完成重入水**：水雷水下扰动／破水与鲨鱼首次上浮已经替换；鲨鱼水花明确使用事件镜头可见层，后续巡游不重复播放入场水花。
4. **已完成水爆与人物分层**：炮火、水雷和定时炸弹共用水爆档及错时水面环；人物受击仍复用角色现有水花，鲨鱼咬伤继续保留定点强水花。
5. **待完成组合真机收敛**：离线类型检查和相关自动回归通过后，仍需按最坏组合做 iOS／Android 微信体验版 A／B，并据实收敛透明面积、最终尺寸、水滴数量和远处寿命。
6. **已接入通用身体碰撞水花**：现有接触开始事件直接驱动独立固定池，标准／狂野／娱乐使用集中阈值和视觉倍率；本项没有修改娱乐道具三档实现，也未新增联机消息。当前阈值与尺寸仍待正常镜头及双端真机收敛。

自动回归至少覆盖：三档入口映射、池满复用、隐藏零更新、20Hz 表现采样、重复 reset／dispose、周期快照不补播、可靠事件去重、独立入口与六合一共用实现、单机玩法判定无变化、网络协议和消息数量不增加。视觉验收须在正常比赛镜头、俯视镜头和事件画中画中检查完整生命周期，不能只截取水花最高的一帧。

## 12. 实施前置门槛

在接入统一事件导演前，必须先完成一次现有六玩法审计并解决以下问题：

1. 水雷目前主要同步命中事件，缺少完整的存活／重生快照；本地漂浮时间也可能在丢包、重连和房主迁移后产生可见偏差。
2. 漩涡现有布局可由种子复现，但统一模式的激活时刻、物理锚点和驻留状态尚无权威阶段快照。
3. 多个 AI 目标目前按固定覆盖顺序写入，尚无统一意图仲裁和迟滞。
4. 六个玩法当前按互斥模式创建，尚不能安全地同时保留驻留控制器。
5. 事件画中画已经共享低分辨率 RT，但需要复核隐藏时相机是否零渲染，以及相机可见层是否绘制了不必要场景。
6. 水雷等驻留表现仍需复核逐帧变换写入、隐藏状态早返回、对象池容量和透明叠加上限。
7. 必须验证比赛重开、退出、房主迁移和快照恢复不会遗留上一事件或上一局状态。

前置问题的修复可以增加快照、降频更新、对象池和复用接口，但不得提前启用事件轮换。所有门槛通过离线检查后，才进入统一导演实现。

### 12.1 现状审计结论

截至 2026-09-19，现有六个独立入口及统一导演的离线问题处理如下：

- 水雷已把权威漂移时钟、存活掩码和出生保护启用掩码加入 `S|` 周期快照；已爆水雷在本局永久失活，不再同步重生计时。可靠命中事件丢失、访客晚加入或房主迁移后，都可由同版本快照修复且不会复活已爆水雷；同一修订的倒序快照也不能回拨导演／炮火计时或复活水雷，旧快照使用安全空状态。统一导演另同步事件数量、事件表、阶段、剩余时间、驻留掩码、特殊事件掩码、激活序号、返场状态、完赛收尾锁定和六个激活锚点；定时炸弹和水雷命中事件保留外围冲击掩码，统一致命击倒另走携带全局 revision 的可靠事件，当前协议为 82。
- 水雷本体表现保持 20Hz；其水下扰动、破水与爆炸已接入娱乐水花共享网格、材质和固定池。隐藏后只在显隐边沿关闭节点并取消本玩法尚未播完的水花，后续帧直接返回。
- 事件画中画已确认共用 256×144 RenderTexture、最高 30Hz，隐藏或非采样帧会关闭相机；赛事文字按状态边沿写入。面板固定在右上排名栏左侧，按安全区与微信胶囊重新布局；标题、状态行和镜头画面使用互不重叠的纵向区域，画中画显示期间赛事广播复用原节点收窄到镜头左侧，隐藏后恢复居中宽度。人物水下着色按视线实际穿过水体的距离计算，高位俯拍不再把空气距离误算为水下蓝色吸收；水线附近保留短距离平滑过渡。水下反射裁切同时校验反射相机位置，主镜头进入水下时不再把水面上的事件画中画误当成反射 pass。当前独立玩法无需再改其渲染路径。
- 心跳苏打、鲨鱼、炮火、定时炸弹已有权威事件与周期快照；独立漩涡由共享种子确定普通／超级布局，统一模式由房主导演特殊掩码恢复规格，比赛结果继续由泳者位置权威纠正。未发现新的独立模式同步缺口。
- 六个入口目前互斥，因此 AI 固定覆盖顺序和控制器共存不是当前独立模式缺陷；它们必须随阶段 B／C 的统一导演和驻留系统一起实现，不能用未启用的空框架冒充前置修复。
- 比赛重开与退出继续销毁现有表现并重建控制器；水雷监听器同时清理命中事件和状态快照。AI 水雷绕行目标在控制器不可用时会为全部本地 AI 清空。

离线代码与自动回归通过后即可进入阶段 B。iOS／Android 微信真机上的透明填充、连续爆炸和双端迁移仍是阶段 D／E 的上线验收项，不以桌面检查替代，也不阻塞导演骨架开发。

## 13. 分阶段实施计划

### 阶段 A：文档与现状基线

- 本文落库，六份独立玩法文档继续作为内部规则来源。
- 完成现有联机、性能和重置审计，修复实施前置门槛。
- 保持六个独立入口行为不变，作为统一模式接入前的回归基线。

### 阶段 B：导演骨架

- 增加统一娱乐入口、专属子种子、200 米三至四事件／400 米五至六事件抽取和生命周期状态机。
- 先只显示广播和阶段诊断，不真正切换六个事件。
- 单机、房主、客机、房主迁移和保活重开得到相同事件表。

状态：已完成。正式入口名为“娱乐模式”；导演使用比赛种子的独立子流保底抽取三类事件，并以 50% 概率追加第四个不重复事件，通过周期快照同步与恢复。

### 阶段 C：公共共存能力

- 建立驻留登记与统一清理。
- 增加 AI 意图仲裁和连锁伤害保护。
- 将事件控制器从“模式存在才创建”改为“统一模式按需启用、驻留时继续运行”。

状态：已完成首版。驻留状态按位登记并在重开／退出清理；AI 使用显式事件意图优先级与 0.35 秒迟滞；急救状态使用跨事件全局递增 revision。

### 阶段 D：逐组接入

1. 心跳苏打与定时炸弹：验证波次压缩和结算门槛。
2. 漩涡与水雷：验证物理位置跨折返驻留和性能上限。
3. 鲨鱼与炮火：验证单轮袭击、结果结算和低速／静态驻留。

每接入一组都保持单事件调试入口可用，不等待六个事件全部完成后才回归。

状态：三组均已接入。统一模式覆盖为苏打 2×3、炸弹 1 轮 8 秒、炮火 3 发、鲨鱼 1 轮、漩涡 1 个、水雷 5 枚；旧单项入口仍复用相同控制器与美术资源。

### 阶段 E：正式开放

- 完成协议升级、弱网恢复、房主迁移和保活重开验证。
- 在 iOS／Android 微信真机检查透明叠加、画中画、长局内存和帧耗。
- 普通界面只展示统一娱乐模式；旧入口转为开发调试或迁移映射。

状态：入口收拢、协议升级和离线回归已完成；iOS／Android 微信真机组合性能、双端弱网与迁移压力测试待设备验收。

## 14. 验收清单

### 玩法

- 200 米从三类各抽一个事件，并以 50% 概率追加第四个；400 米抽取五至六个不重复事件；场地事件都不在最后。
- 每次广播提供 5～6 秒预告，并在激活后显示约 3 秒红色行动提示；上一事件静默收尾后进入 200 米至少 5 秒、400 米至少 8 秒的喘息，并等待下一赛程锚点。
- 六种事件各有十组黄色预告／红色行动提示；同一种子、事件和激活序号在各端得到相同组号，同一事件前十次取样不重复，展示期间不换句。
- 不会同时运行两个爆发阶段。
- 主事件覆盖至赛程后段；比赛继续且无人完赛时可安全返场炮火、鲨鱼或定时炸弹，相邻返场不重复；首位选手触壁后立即取消未激活预告，不再产生新事件，已激活事件自然结算且驻留障碍保留到本局结束。
- 各事件按压缩后的数量停止生成，指定残留物在后续事件和折返后仍有效。
- 可见物与判定一致；比赛结束和重开无残留。
- 炸弹在急救、无敌、冲线和事件切换边界行为符合本文。
- 已完赛选手不再成为事件目标。

### 联机

- 房主和所有客机事件表、阶段和剩余时间一致。
- 房主和客机使用既有比赛种子与导演激活序号确定广播组号，不传输文案字符串，也不增加快照字段。
- 丢失事件开始消息后可由周期快照恢复。
- 帧同步可用、广播兜底和混合网络三种路径均能去重。
- 在广播、炸弹倒计时、水雷漂浮和鲨鱼追猎期间迁移房主，事件继续且已爆水雷不会恢复。
- 断线恢复和保活重开不会继承上一局驻留物。
- 单机和旧独立玩法在未选择统一模式时没有额外网络或逐帧成本。

### 性能与 UI

- 节点、组件、监听和对象池数量在多轮事件后保持稳定。
- 六个事件不存在六合一专用的重复模型、材质、Prefab 或表现分叉；单玩法公共美术调整可直接进入六合一。
- 隐藏画中画、隐藏 HUD 和未激活事件控制器没有比赛帧工作。
- 热路径不存在无条件 `Label.string`、`Graphics.clear()`、重复 Tween、重复 `Node.active` 或临时对象分配。
- 同时显示漩涡、水雷、苏打和广播画中画时，透明排序和帧耗通过 iOS／Android 微信真机验证。
- 离线类型检查和自动测试通过；桌面结果不替代真机性能结论。

### 14.1 2026-09-19 分支离线复核结果

- 统一击倒已补充房主权威可靠事件，携带泳道、原因、权威距离和全局 revision；`S|` 急救快照继续承担广播丢包、晚加入与房主迁移恢复。
- 微信 iOS 高性能构建策略已收敛成一个受版本控制的开关。帧通道绑定或上传失败时锁存为不可用，并用 `NB|` 通知混合设备房间统一切换到 `IN|` 输入事件和 `P|` 自状态广播；相关构建、降级和文档契约已加入 `test:net`。
- 当前八泳道 `S|` 报文实测：常规空闲状态约 762 字节，八泳道同时携带姿态、软碰撞与角色能力的高载荷状态约 1234 字节，人工极端字段约 1441 字节；自动测试以 1536 字节作为项目回归预算，房主快照保持 0.15 秒间隔。该数值是内部增长警戒线，不等同于微信平台公开上限；后续增加字段前必须重新测量，赛内网络调试 HUD 与逐帧日志默认关闭。
- 画中画固定为 256×144、最高 30Hz；世界表现固定 20Hz；炮火和定时炸弹 HUD 固定 10Hz。鲨鱼锁定标记缓存投影组件，并只在量化位置或缩放变化时写节点变换。
- TypeScript、娱乐玩法、联机、平衡、房间、碰撞姿态、字体、纹理和商店图片离线检查均已通过。iOS／Android 体验版双机、弱网、房主迁移以及画中画透明填充和发热仍为设备验收项。
- 超级漩涡已共用独立／六合一控制器：独立模式 25% 概率强化第二或第三个漩涡，统一模式条件概率 25% 并由导演快照恢复；中心水雷保留区、画中画让位和赛前资源预热已加入离线回归。

## 15. 已确认与待调参数

已经确认的方向：统一入口、200／400 米双赛程、短局随机三至四个事件、长局随机五至六个事件、三类各保底一个、每种事件十组确定性随机搞笑广播、提前 5～6 秒黄色预告、激活时同组单行红字持续约 3 秒、按赛程锚点分散事件、事件间保留真实喘息、无人完赛时安全返场、首位完赛后停止新排期并让当前事件自然收尾、旧场景麻烦有限驻留、水雷触爆后本局永久消失、漩涡事件条件概率出现一个泳池中心超级漩涡、爆发阶段不重叠、击倒后急救重生、房主权威和驻留数量硬上限，以及娱乐道具水花按轻落水／重入水／水爆三档共用美术基线精修。

上线前仍可调但不阻塞开发的内容：第一段普通游泳时长、各组赛程锚点、5／8 秒最短喘息、10～12 秒返场间隔、炮击间隔、第二波苏打间隔、鲨鱼巡游速度、低性能档画中画刷新率，以及三档水花的最终尺寸、辅纹强度、水滴数量和远处可见期。

当前仍需重点做一轮真机节奏验收：400 米串行安排五至六个事件，并把爆发时长放大到短局约 140%，在较快完赛速度下可能来不及完整激活事件表；返场还需要 10～12 秒喘息和完整预告，正常比赛通常不会触发，它只是超长局兜底。这里不能只靠减少预告或反应时间硬塞，应在真机记录实际 400 米完赛时长后，再在“保留五至六个事件”和“保证每个事件完整展开”之间确定最终取舍。
