# 已撤销：CartonSwimmer14 的 bf613 误替换记录

> **本次目标判断错误，用户已纠正为「绿电潮童」CartonSwimmer12。** 下文仅保留误替换的历史过程，不是当前游戏状态或有效导入指令。霓绿少女 14 已从备份恢复为 c1b6 精修版；新 bf613 的正确接入及验证以 [绿电潮童替换说明](character-replacement-carton-swimmer12-bf613.zh.md) 为准。

日期：2026-09-05。用户先确认 `bf613b96` 源精修版，再明确要求「替换进游戏内」。本次只替换已有 `cartonSwimmer14` 的模型和换色遮罩，不新增角色、不修改属性、角色 ID、存档、默认选择、联机协议或共享动作。

显示名和说明文案暂时仍沿用原槽位的「霓绿少女」；新模型为黑灰头发的少年外观。此次未擅自改名或重新设计。

## 来源、成品与回退

- 用户确认源：`tools/characters/bf613_cleanup/Character_FineRefined_v1.glb`，SHA-256 `90d513deb9869cfc318cbc184bbf2c523a9778e03e95ff33fa83a4e8ca169857`。
- 原始桌面 GLB 未改，SHA-256 `53859addc54640cd92e16cc56ce00de45198ba0a5b8ed71c23678a87661d41e4`。
- 运行模型：`assets/race/models/CartonSwimmer14.glb`，642292 字节，SHA-256 `c761f344486866afde16bec0849bd3e32ba25c147b84242dc38fc804207adc33`。
- 换色遮罩：`assets/race/models/CartonSwimmer14ColorMask.png`，58171 字节，SHA-256 `2db8b3590a0a732a6859f7e1f198cf9b2dbe133599775fe88435bfc1736b3852`。
- 旧 c1b6 运行模型、遮罩及两份 `.meta` 均保存在 `tools/characters/bf613_cleanup/runtime-backup-c1b6/`，其中 `manifest.json` 记录原字节数和哈希，可作为恢复来源。
- 新运行打包候选、结构报告和动作图在 `tools/characters/bf613_cleanup/runtime-import/`；贴图制作与换色预览在同级 `runtime-textures/`。源文件、备份和预览没有进入 `assets/`。

两项运行文件合计 700463 字节，较旧版 958071 字节减少约 26.9%。这是源资源文件大小，不是微信最终压缩包或 GPU 内存的实测结果。

## 模型与动作

保持 1 网格、1 材质、1 primitive、4085 顶点、5525 三角、41 骨骼。沿用原 UV，不减面、不转移权重、不挪动肩、肘、腕等关节中心。只规范整体朝向及标准骨骼轴，按角色自己的关节中心更新父级位移和逆绑定。

原骨轴与标准骨轴存在明显差异，不能仅凭同名骨直接导入。规范化候选保留 UV、关节索引、权重和三角索引的原字节；整体朝向转换后所有关节位置误差为 0。最终打包只替换 512 图片，其他数据再次逐字节比较通过。

继续使用 `model-actions/tPose`，没有为此角色复制动作目录或修改比赛姿态算法。21 项共享动作／静态准备姿势共 3661 个样本数值通过：无缺骨、非有限值、额外 Root 旋转或相对自身关节参考的左右手序差异；独立蒙皮参考最大偏差约 `3.155e-6`，静置有效骨轴最大偏差约 `0.000194°`，动作全骨朝向最大偏差约 `0.000275°`。

已查看 39 张离线动作图：九个高风险动作各五帧、前／侧／三分之四视角，跳水准备三视角及肩肘掌近景。没有发现明显翻掌、镜像、肩部异常膨胀或前臂扭断。对照图为便于观察而调整了展示位置，不能作为游戏接地证明。

## 运行贴图与换色

源美术图保持 2048×2048；运行底图与遮罩均为 512×512。直接缩图曾产生 UV 黑裂，该候选被拒绝。最终在原 UV 上进行 2048 烘焙、8 像素扩边后缩图，底图为无损 RGB PNG，SHA-256 `ec2070cacf33ec15cff169c9473c5c56e55d2030c745096282f9d1a5c34ea326`。

绿色设计区通过本模型的连通部件、空间范围及原色共同确定。头发、皮肤、金属环等即使有绿色样本，也不自动纳入。R 用于帽、衣、裤侧片、鞋面、腕带、腰扣及侧包换色，G／B 全零、A 全 255；不启用独立帽色或肤色覆盖。白条、内搭、袜子、鞋带、蓝镜片／屏幕／闪电挂件、黑灰头发和金属固定。浅色帽标单独保护，首轮换色吞掉帽标的候选已淘汰。

只离线调整本模型遮罩的分数覆盖编码，适配现有着色器的 `smoothstep(0.015, 0.12)`；最终 PNG 重新读取后再以线性过滤核验。没有修改公共着色器、新增材质槽或增加逐帧纹理处理。原色和红、蓝、黑、黄、紫五色经过全身及局部检查，另有独立复核。

512 极近景仍可见少量底绿混合软边，以及镜片和鞋底的像素软锯齿；不能声称与源 2048 近景完全一致或完全无缝。源版认可的真实低模切面、帽口／鞋舌局部软边仍保留，不以扩大擦除或整体涂平替代。

## Cocos 与自动检查

- 实际覆盖上述两项运行文件，保留原 `.meta`；Cocos 已更新导入数据。
- 模型父 UUID `d68b1e8c-ab7a-4857-80ce-5d54779503a0`、遮罩 UUID `741fd71d-517d-446f-8cf9-b510d1aee884` 不变，场景名称保持 `CartonSwimmer14`。
- 已核对 `library` 中的新网格为 4085 顶点／5525 三角，图片和遮罩哈希与最终文件相同；预制体→网格／骨架／材质→纹理／图片引用完整。
- 导入的 41 根骨骼顺序一致，位置和逆绑定矩阵最大误差均为 0，局部旋转最大误差约 `0.00000342°`。
- 仅激活已经运行的 Cocos 触发扫描，没有启动、重启或截图编辑器。
- 在元数据更新后执行 `textures:fix` 与 `textures:check`，扫描 159 项通过。底图保持 ASTC 6×6＋JPG 回退，遮罩保持 ASTC 6×6＋PNG 回退，GLB 元数据及实际导入 Texture2D 都关闭 mip 采样。
- 角色回归 15 项通过，网络回归 14 项通过，TypeScript 5.4.5 类型检查和字体检查通过。没有新增运行时代码或中文界面文案。

数值证据：`runtime-import/cocos-import-report.json`、`shared-actions-detailed.json`、`preserved-buffer-report.json`、`CartonSwimmer14_Runtime.validation.json`；贴图证据：`runtime-textures/runtime-texture-audit.json`。上述路径相对本角色的制作目录。

## 仍需游戏验收

重新打开已有游戏预览，在原「霓绿少女」槽位或 Debug Model 选择该角色。已加载的旧模型实例不会因为磁盘资源改变而自动重建。

请核对准备展示、跳水、自由泳／踩水、过渡、脚底接地和换色近景。本次没有实测程序化自由泳、跳水飞行、状态过渡或通用两骨 IK；未经运行求解的离线支撑脚最大偏差约 0.0431 模型单位，不记作接地通过。微信构建、ASTC 压缩外观、iOS／Android 实机也未验证。

仅离线美术资源替换，不新增网络状态或影响比赛结果的参数。原联机头像映射保持不变，不承诺联机真人外观自动选择本角色。未上传外部平台、提交或推送仓库。
