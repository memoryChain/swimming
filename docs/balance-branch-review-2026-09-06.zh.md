# balance-tuning 近两周合入筛选

核对日期：2026-09-06。时间范围：2026-08-23 至 2026-09-06。

已执行 `git fetch origin --prune`。审查基线：主干 `bbb65b7`，分支 `bb3ff10`。实际分支名为 `balance-tuning`。

## 后续采纳记录

用户确认采纳前六项，包含海豚跳消耗从 30 改为满槽 100。已在主干移植 `c9e4bec`、`846b9b3`、`5e50afa`、`5ed04b7`、`d258a5d`、`fc02ba5`。

- 保留主干的角色、结果页、碰撞柔性反馈和网络字段；增加远端真人碰撞身份标记，联机玩法版本升至 14。
- JSON 保留主干配置，只更新满槽消耗、侧墙参数和正面碰撞脱困参数；录制按钮与中文调参面板采用随包字体。
- 类型检查通过；调参与侧墙/能量回归 4 项、网络与碰撞 26 项、房间/结算/主干布娃娃回归 40 项通过。
- 已运行字体生成、字集检查和纹理审计；新增 `pnpm test:balance` 作为这批平衡回归的入口。
- 未合入后续的双形态海豚技能、分支主动布娃娃、冲刺心率改动、奖励翻倍、准备页拆分或隐藏蓄力条。
- 尚需真机确认起跳 shader 观感、侧墙/正撞手感和双设备表现。以下内容保留为合入前的审查记录。

## 结论

- 祖先关系上，分支比主干多 36 个提交：33 个普通提交、3 个合并提交。
- `git cherry` 确认其中 14 个普通提交的等价补丁已经进入主干；真正需要筛选的是剩余 19 个。
- 建议优先选择小而完整的调试工具、起跳效果修复和碰撞改善；海豚双形态、冲刺新平衡、奖励翻倍另作功能包。
- 不建议直接合整个分支：无工作区写入的合并模拟发现 34 个冲突文件，而且主干已经更新了角色、界面和碰撞表现。
- 下文“完成度”指代码链路、边界处理和现有测试的成熟程度，不代表真机验收或平衡性已经通过。

## 第一批：优先筛选

| 功能 | 提交 | 完成度与实际变化 | 合入建议 |
|---|---|---|---|
| 录制净屏模式 | `c9e4bec` | 高。开发模式按钮隐藏性能统计、日志、AI 调试面板和调参入口；跨场景维持当前会话状态，重启复原。实际是隐藏调试 UI，不包含视频采集或录像导出。 | 优先。单提交合并模拟无文本冲突。 |
| 翻滚转身调参中文化 | `846b9b3` | 高。已有参数名称与说明中文化，稳定参数 ID 保留。 | 优先。单提交模拟无文本冲突；按主干字体流程更新和检查静态字集。 |
| 起跳蓄力效果生命周期修复 | `5e50afa` | 较高。起跳已确认后阻止再次启动聚气，避免空中点击重启特效；同步调整聚气密度与 shader 表现，继续使用单个复用网格/材质。 | 优先。单提交模拟无文本冲突。前置释放时机修复 `5981a55` 已在主干。效果外观和移动端开销仍需真机确认。 |
| 侧墙反弹柔化 | `5ed04b7` | 较高。限制撞墙回正角速度，用阻尼方式转向较小脱墙角，并停止已完成的回正。参数已接调参面板及 JSON。 | 优先作为手感候选。单提交模拟仅 `tuning.json` 冲突；尚无针对该墙体恢复模型的专门测试。 |
| 正面碰撞侧向脱困 | `d258a5d` | 较高。对向相撞时增加稳定的横向分离，保持已有左右关系，持续接触只施加一次冲量；处理重合位置与遍历顺序问题。 | 推荐。分支最新状态的 5 项碰撞脱困测试通过。但主干碰撞已变化，需保留主干软体状态逻辑再移植。 |

“模拟无文本冲突”仅说明使用该提交父版本作为合并基底时 Git 可以自动合并，不代表移植后的项目已经编译或完成验收。

## 第二批：值得吸收，但需要拆分或补验证

### 调参校验与实时刷新：`fc02ba5`

代码完成度较高：增加未知键和非法值诊断、取值范围限制、旧参数迁移、参数关系校验；单机修改部分速度和推进参数后重新计算角色养成覆盖，避免面板改变而玩家行为不变。联机路径有隔离。

**此提交并非纯工具修复。** 同时把 `dolphinCost` 默认值从 30 改成 100，要求消耗与能量上限一致，并把释放动作改为直接清空能量。当前主干仍保留 30 点扣费模型。

建议先吸收校验、迁移和实时刷新；“满蓄气才能释放、释放后清空”作为独立玩法选择。单提交模拟仅 `tuning.json` 冲突，但没有专门的调参回归测试。

### 结算广告双倍奖励：`30b5af0` + `683d866` 中的奖励部分

本地实现链路较完整：结果入口、看完广告后领奖、结算凭据、重复领取保护、最多 32 条凭据、持久化失败重试，以及失败响应不覆盖当前存档。已看完广告但领取失败时，同一会话重试可跳过再次看广告。

两个纯逻辑测试通过，但只覆盖存储失败上抛和失败领取不回滚存档，不是广告与持久化的端到端验收。微信及抖音广告位仍为占位值；现有后端为 localStorage 的 MockBackend。

建议作为单独的奖励功能包。把 `683d866` 的奖励修复一起带上，但不要因此顺带引入该提交里的 AI 海豚同步。主干已重做结果界面和存档字段，需要接到现有结果页，保留当前角色与外观持久化。

### 冲刺心率与推进收益：`7b47d11` + `bb3ff10`

实现包含连续游泳负荷、冲刺心率渐进上升、明确的冲刺推进倍率、可调参数、大厅调参指纹，以及远端真人冲刺状态回放。两次提交必须成组考虑；只拿前一个会漏掉后续联机修复。

当前 2 项大厅协议测试仍以旧接口调用，缺少指纹参数；新冲刺状态和指纹行为也没有相应新增测试。需要修正测试并补玩法与双设备验证。

**协议不能直接覆盖：** 主干协议号为 13，分支为 10；更关键的是可靠输入 self 数据的索引 15，主干已经用于 `collisionSoftness`，分支用于 `sprintActive`。需要同时保留两者、重新安排追加字段并统一升级协议，不能仅取较大的版本号。

建议第二批单独移植，在主干新版房间 UI 上接入指纹门禁，不替换整个 `RoomFlow`。

## 第三批：完整玩法原型，暂不直接合入

### 海豚跃角色差异化与出水/深潜双形态

核心提交：`16020fb`、`6364a14`、`683d866` 的 AI 海豚部分、`6c41497`。

包含角色充能与轨迹倍率、触发姿态判型、墙前拒绝与压缩轨迹、碰撞开关、镜头、泡沫尾迹、AI 决策、房主接受动作、动作去重和权威起点重放。代码链路较完整，不只是表现演示。

当前限制：

- 差异化配置按旧的 `muscleMan / women2 / lowPolyHuman2 / diver` 档案编写；主干现在是 11 个角色，新 ID 大多只会落入默认档案，不能直接视为现有角色差异化已完成。
- `docs/dolphin-jump-character-balance.zh.md` 是首轮目标与空白记录表；`docs/dolphin-dive-dual-form.zh.md` 是设计与验收清单，不能当作已完成的实测结果。
- 双形态文档仍写协议版本 4，分支最新代码已到 10，文档需校正。
- 网络编解码、去重和起点相关测试有通过证据；完整轨迹、边墙、不同角色和双设备表现未在本次验收。

建议先决定是否采用双形态，再重新映射当前角色。移植时保留主干最新模型姿态与脚部接触修复。

### 碰撞主动布娃娃及海豚技能姿态继承

核心提交：`37cb3e5`、`8c5253d`、`cd8cb40`、`39acc67`。与上述海豚玩法和 `6c41497` 的触发姿态锁定相互关联。

包括碰撞四肢反应、反应衰减、关节角度限制、稳定种子、显式同步事件、迟到补偿，以及海豚动作内嵌布娃娃快照。15 项控制器测试和 3 项倒置恢复测试通过，相关网络测试也通过。

**主干已经在 `046a8ef` 实现另一套 `CollisionLimpPoseController` 与碰撞柔性同步。** 分支这一套的联机事件与姿态继承更丰富，但两套系统会触及相同的身体和关节，不适合叠加。需要先选用或统一一种实现，并在当前角色骨架上验证。完成度较高，集成成本也高。

## 不建议作为独立候选

- `0f136a6` 准备界面拆分：有页面复用、局部刷新和角色预览身份检查等可取做法，但主干之后已经重构大厅、角色页、房间及美术资源。整体移植收益低，容易冲掉较新的 UI。
- `445b7db` 隐藏起跳蓄力条：实现很小且完整，但属于产品表现选择。依赖玩家能从聚气效果读懂蓄力，不能归类成必合修复；主干 `UIController` 已变化。

## 已经在主干的 14 个等价补丁

经 `git cherry origin/main origin/balance-tuning` 识别，下列提交无需重复合入：

| 分支提交 | 内容 |
|---|---|
| `d553ce3` | 碰撞俯仰惯性调参 |
| `475d96f` | 降低基础比赛节奏 |
| `f1e7e9c` | 划水体力消耗调参 |
| `2838ecb` | 增加心率惯性 |
| `8243a97` | 体力耗尽恢复冷却 |
| `5287cac` | 权威体力/心率状态传输 |
| `baec7fd` | AI condition 固定时钟 |
| `57192f6` | 远端输入前应用 owner condition |
| `4b80165` | 力竭时降低划频 |
| `9485994` | condition 同步与输入重放顺序修复 |
| `27145a4` | 可滚动赛中调参面板 |
| `5b9c392` | 海豚跳心率负担 |
| `5981a55` | 起跳前释放蓄力效果 |
| `8cc9999` | 大厅版本声明丢包重试 |

## 本次验证范围

- 在系统临时目录导出 `balance-tuning` 源码，复用本机 Creator 3.8.8 生成的类型声明；没有切换工作区分支。
- 分支执行 TypeScript 5.4.5 检查通过：`tsc --noEmit --ignoreDeprecations 5.0 --skipLibCheck`。
- 执行网络编解码、condition 固定时钟、奖励持久化、碰撞俯仰、布娃娃控制器、碰撞脱困六个测试文件：46 项中 44 项通过、2 项失败。
- 失败位置：分支 `tests/net-race-codec-ordering.test.mjs` 第 299 行和第 311 行的大厅测试。最新接口要求调参指纹，测试没有同步更新；该结果本身不证明实际大厅一定无法开局。
- 使用 `git merge-tree` 模拟整体与部分单提交合并；没有实际 merge、cherry-pick、修改业务代码或推送。
- 未启动 Creator、未构建微信包、未做真机视觉/性能/双设备验收。

建议筛选顺序：录制净屏与中文化 → 起跳效果修复 → 侧墙与正面碰撞改善 → 拆出的调参工具修复 → 奖励与冲刺功能包 → 海豚双形态及统一后的布娃娃系统。
