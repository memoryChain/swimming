# 铁臂狂鲨精修版接入记录

2026-09-06，按用户确认的短臂精修 v3 替换 `muscleMan`。文件已安装，Creator 导入尚待刷新，不作为引擎运行或真机通过报告。

- 运行模型：`assets/race/models/MuscleMan.glb`，保留 UUID `cf3994cf-aca9-45c3-8bf1-9ca1b3d2790a`。
- 原模型 SHA256：`2b9e9f92b4de631949c5e2392f6203cfce28574dc9daabbcaa094acd1be3b2f0`。
- 确认源 SHA256：`69a9445e4e7705df5227240228fbb68f9b20448d45f4ccfee93a0181b2b364ab`。
- 安装模型 SHA256：`9f7da7894c9d4c8c0c35525dd5d0ad895e33e5c3c745de0a23ad32fb73f54536`。
- 新增 `MuscleManColorMask.png`，SHA256：`0283e13015ea909b2a03e6c0731cd0352111cf9f82853a374960f57fd959a61d`。
- 单蒙皮网格、单材质、41 骨骼、3682 顶点、5526 三角面；512 底图及512遮罩。GLB 611136 字节，遮罩51786字节；旧GLB207336字节。资源位于race分包，未增加网格或材质槽。
- 衣裤R、泳帽G、露肤B；白背心、绿色泳镜及灰绿镜带保护。服装通道按现行effect阈值编码，皮肤通道使用线性覆盖。沿用既有衣帽配色方案、原肤色/深肤色和存档链路。
- 保留角色等级、属性、稳定ID、1.12倍配置与卡面插画。源几何高约0.83057，旧模型约0.96680；加载器无自动身高归一化，本轮保持用户确认源比例，未再调整体型。
- 对源坐标仅做刚体朝向旋转；规范化骨骼局部轴与逆绑定矩阵，保留关节中心。UV、蒙皮索引/权重、拓扑及内嵌底图字节不变。
- 联机继续使用既有头像→稳定角色/颜色/肤色映射，未扩展协议。未将本地自选外观声明为已经跨端同步。

## 验证结果

- 蛙泳91帧、准备1帧、挥手143帧离线采样，无缺骨骼和非有限顶点；抽样回贴检查姿态。未代表完整比赛显示验证。
- 类型检查通过，使用本地缓存 TypeScript 5.4.5，参数 `--noEmit --ignoreDeprecations 5.0 --skipLibCheck`。npx在线解析等待后改用相同版本本地入口。
- 接地、起跳台和头顶边界69项通过；本模型站立最大鞋底偏差0.49毫米、斜台0.10毫米。
- 角色全量回归19/20通过；唯一失败为未修改的“深潜先锋”资源哈希断言（预期f475b243…，实际1f84a8d6…）。本轮铁臂狂鲨独立回归通过，最终遮罩更新后再次通过。
- 现有导入元数据的 `textures:check` 通过；此检查不涵盖尚未生成meta的新遮罩。等待Creator更新后再执行 `npm run textures:fix` 和 `npm run textures:check`。
- 只读核对library仍为旧2080顶点/3006面，新遮罩meta尚不存在。没有手改library或伪造导入meta，没有启动/重启Creator。
- 离线回贴检查原/深肤色、衣帽换色及头部、袖口和手部近景。512纹理的极近景边界仍受原UV与分辨率约束；未提升运行贴图分辨率。

## 待完成的引擎验收

在现有Creator中刷新模型和新遮罩资源，待meta生成后运行压缩策略修复/检查，并按UUID核对新网格、骨骼与遮罩绑定。再查看选角、比赛、换色、重新进入的外观保持，以及微信iOS/Android压缩显示。本轮未执行引擎预览、构建或真机验证。

候选、备份、数值报告与离线预览位于 `tools/characters/bf8d2b32_cleanup/runtime-import/`。原始模型未覆盖，未提交或推送。

## 用户确认后的整体放大

按用户“这个角色要放大点”的要求，`muscleMan.modelScaleMultiplier` 从1.12调整为1.24，整体放大约10.7%。沿用共享预览/比赛缩放配置，短臂比例、模型文件与换色遮罩不变；联机同角色也读取此配置。
