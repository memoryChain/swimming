# 精修 v3 角色游戏接入

> 2026-09-06 更新：已补充皮肤换色，`supportsSkinTone=true`。下文 2026-09-05 的固定肤色和旧遮罩哈希属于初次接入记录；当前变更及验证见文末「肤色功能补全」。

> 目标更正：用户明确 bf613 精修版应替换「绿电潮童」12，而不是「霓绿少女」14。14 已恢复为本文记录的 c1b6 精修版，模型与遮罩哈希再次对应下文。正确的 bf613 接入见 [绿电潮童替换说明](character-replacement-carton-swimmer12-bf613.zh.md)。

日期：2026-09-05。新增 `cartonSwimmer14`，暂用显示名「霓绿少女」。本角色来自 `c1b6e588`，不是 `ee5684b1` 的「深潜先锋」，不覆盖已有角色，也不修改玩家默认选择或已有存档的稳定 ID。

## 来源与资源

- 已确认源模型：`tools/characters/c1b6_cleanup/Character_FineRefined_v3.glb`。
- 源模型 SHA-256：`5a12d5c96067f479e0cb48a1b5be74b42c4afb3d43156aa3644ca5d9ef07fb75`。
- 离线骨架候选与检查：`tools/characters/c1b6_cleanup/runtime-import/`。
- 运行贴图候选与同机位对照：`tools/characters/c1b6_cleanup/runtime-textures/`。
- 游戏资源：`assets/race/models/CartonSwimmer14.glb` 与 `CartonSwimmer14ColorMask.png`。
- 制作文件与预览不进入 `assets/`；原始文件和精修源版保留。

最终模型 898692 字节，遮罩 59379 字节，合计 958071 字节（约 936 KiB），均位于 `race` 分包。模型 SHA-256 为 `bacc550794c2bceff1a188cfd16b01cea3d8a955644f84c70e1b76264cffa32d`；遮罩 SHA-256 为 `2ff191e9d86fc41034d59c2308dbe5120b18736d05e60c8fcc2fa0783d652f28`。

## 贴图与换色

2048×2048 精修版保留为源美术文件。运行底图沿用 512×512，保持原 UV，不因导入重新设计、焊接、减面或重排图集。

直接缩图会把黑色岛间底色采入模型，引起新的三角黑缝，因此该候选被拒绝。运行候选先在原 UV 进行 2048 超采样烘焙、8 像素岛边扩边，再缩到 512；白袜、灰白头发、胸条、裤脚滚边和配件图案均需回贴核对。极近距离的分色边缘较 2048 源版柔和，不承诺源版与运行版逐像素相同。

本次优先使用无损 PNG 底图，源 GLB 文件预算控制在 1 MiB 内；这是在保持原 UV／网格与精修边界前提下的本角色包体取舍，不提高纹理分辨率、GPU 纹理尺寸或通用角色预算。平台压缩仍遵循项目统一策略。

换色复用现有 `SwimmerDynamicColor.effect` 与一次加载的共享遮罩，不添加逐帧纹理处理或新材质槽：

- R：已标定的绿色服装、帽子、鞋面和装备区域，共用服装色。
- G／B：零；A：255。不启用独立帽子或肤色换色。
- 白袜、灰白头发、源肤色、黑白结构、紫色镜片／屏幕／配件保持原色。
- `supportsSkinTone=false`，不让肤色切换破坏已精修的固定区域。

## 骨架与动作

运行版只统一整体朝向与标准骨骼轴，保留新角色自己的肩、肘、腕及所有关节位置、蒙皮权重与 UV。复用 `model-actions/tPose`，不复制动作目录，不修改共享动作或主比赛姿态算法。

最终使用 `CartonSwimmer14_Preserved.glb` 作为骨架打包输入，保持 1 网格、1 材质、7753 顶点、5603 三角面及 41 骨骼。UV、关节索引、权重和三角索引保留原字节；整体朝向使用精确轴置换，再按原关节位置更新标准局部轴与逆绑定。重新导入后的有效局部骨轴误差最大约 0.000135°。

不同身材的角色即使骨骼轴一致，手掌在世界坐标中的交叉时刻也可能不同。因此诊断时同时核对骨骼旋转、实际掌面、关节比例与高风险姿势，不能仅凭与 MuscleMan 的手序差异判定翻手，也不能只凭骨骼同名宣称兼容。

## 代码与联机范围

角色表与资源表供角色选择、准备预览、比赛模型和 Debug Model 共用。角色属性沿用现有均衡角色，不新增能力或赛中逻辑。

在原工作树协议 11 基础上升至 12，防止新旧角色／养成配置混跑。既有联机真人外观仍由头像稳定 ID 映射，不因本地选择新角色自动切换为 14；本次没有改变头像映射、房间生命周期、数据通道或逐帧网络逻辑。

新增中文随项目字体构建流程更新，不手改字形清单或字体。已有其他任务的 UI／网络更改保留，未打包发布或推送仓库。

## 验证记录

- 类型检查：TypeScript 5.4.5、`--noEmit --ignoreDeprecations 5.0 --skipLibCheck` 通过。
- 网络回归：14 项通过；未新增网络热路径。
- 完整角色回归：15 项通过，覆盖新角色选择、保存 ID、服装色切换、固定肤色、资源结构与旧角色表协议门禁。
- 字体：`fonts:build` 与 `fonts:check` 通过。
- 离线动作：21 项共享动作／静态准备姿势共 3661 样本，无缺骨及非有限旋转、位移或变形坐标；与自身原关节加独立标准骨轴参考的手序差异为 0，变形顶点差最大约 3.47e-6。九个高风险动作各五帧及跳水准备三视角已检查。原始脚底数值尚未执行游戏通用两骨 IK，不标为接地通过。
- 换色：原色与红、蓝、黑、黄、紫的正背左右及重点近景已离线检查。遮罩为 512 RGBA、R 换衣、G/B 全零、A 全 255；无损增加 A 通道没有改动已验收的 RGB 值。帽檐／发根、衣缝及短裤仍有少量源版细线，极近景亦存在有限像素边，不宣称完全无缝。
- Cocos 已生成并导入最终模型和遮罩子资源；模型 UUID 为 `d68b1e8c-ab7a-4857-80ce-5d54779503a0`，遮罩 UUID 为 `741fd71d-517d-446f-8cf9-b510d1aee884`。`library` 中图片与最终文件哈希一致，材质纹理引用正确，导入关节位置误差为 0、局部旋转误差最大约 0.00000342°。编辑器与预览脚本缓存中均已出现 `cartonSwimmer14`。
- 通过当前已有编辑器窗口触发资源扫描，没有启动、重启或截图 Cocos。自动生成元数据后执行 `textures:fix` 与 `textures:check`，扫描 159 项、纳入压缩 71 项并通过；底图 ASTC 6×6＋JPG 回退，遮罩 ASTC 6×6＋PNG 回退，GLB 的元数据与实际导入 Texture2D 均关闭 mip 采样。
- 完整制作报告位于 `runtime-import/shared-actions-detailed.json`、`runtime-import/preserved-buffer-report.json`、`runtime-import/cocos-import-report.json` 和 `runtime-textures/验收说明.md`；路径均相对本角色的 `tools/characters/c1b6_cleanup/`。
- 微信平台构建和 iOS／Android 实机未执行；离线图片不冒充游戏截图。

## 用户验收入口

在角色列表或 Debug Model 选择「霓绿少女」。本次不自动改写当前选中角色。请在现有游戏会话中确认准备展示、跳水、自由泳／踩水、鞋底接地与换色近景；这些实际运行环节和压缩后的真机效果仍需验收。

## 肤色功能补全（2026-09-06）

用户要求霓绿少女与其他人形角色一样提供肤色选项。本次在既有 512×512 RGBA 遮罩的 B 通道加入面部、颈部、双臂、双手及裸露腿部覆盖，开启 `supportsSkinTone`；「黄」保留原肤色，「黑」复用现有深肤色参数。服装与肤色独立切换，预览仍走 `applyAppearance()` 更新材质，不重建模型或重启动作。

- 模型、UV、骨骼、底图与两份 `.meta` 均与备份逐字节一致。遮罩 R／G／A 逐像素不变，只增加 B 通道；银发、白袜、紫色镜片与配件保留原设计。
- 遮罩候选通过模型空间裸露区域与暖色底图交集生成，4 倍采样后按面积降采样。不扩张 UV 岛，不增加材质、贴图数量、渲染批次或逐帧工作。
- 新遮罩 79715 字节，比原版增加 20336 字节。SHA-256：`e82c22cc34f6cd3f0230c995c8f2b1925836e09e223a101fcb41c7f22fa187ed`。GLB 哈希不变。
- 复现顺序：`scripts/audit-cartoon-skin-masks.py`、`scripts/repair-cartoon-skin-masks.py`、`scripts/render-cartoon-skin-validation.py`，均传 `--workdir <独立工作目录> --ids 14`；审计与渲染通过 `scripts/run-blender.py -- --python <脚本>` 执行。候选生成不会直接覆盖运行资源。
- 本机备份、几何审计、候选和原色／五组高对比装备色、正背左右、蛙泳／挥手姿势检查图位于 `temp/neon-skin-20260906/CartonSwimmer14/`。采用当前深肤色 RGB 118／76／58。极近景仍可见少量原碎片 UV 的细线，不将其描述为完全无缝；没有重新设计或改变原服装遮罩。
- 验证通过：TypeScript 5.4.5 类型检查；16 项角色回归，包含反复切换肤色与服装、恢复原肤色；`textures:fix` 和 `textures:check`（188 项扫描／98 项纳入压缩）；模型、元数据及未修改通道一致性检查。
- 联机仍从共享头像 ID 派生外观，当前头像映射不包含 14。本次不改房间协议或网络状态；本地肤色选项不会改变既有联机外观映射。
- **尚未验证**：当前 Cocos `library` 中仍为旧遮罩，须由已有编辑器会话扫描／重新导入该 PNG 后刷新预览；本次未启动、重启或截图 Cocos，也未手写其导入缓存。微信 iOS／Android 压缩后效果及引擎内反复切换、预览动画连续性仍需实测。
