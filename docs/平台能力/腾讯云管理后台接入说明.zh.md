# 腾讯云管理后台接入

## 使用方向

使用现有环境 `cloud1-d5gn11660fac3375c` 的**腾讯云官方控制台 → 文档型数据库 → 集合管理**，直接可视化查看和编辑数据库。负责人需要的是数据库操作界面，不需要独立网站；不再以微搭发布或配置自有域名作为日常管理的前提。

2026-09-27 按负责人要求，已删除本地后台源码、专用依赖、构建产物、启动命令、专用测试、预览文件和旧使用说明，并停止两个本地服务。微信游戏云存档、管理云函数、审计和管理请求生成脚本保留。

## 现有身份和数据

- 云函数 `swimming-admin` 的 `ADMIN_PLAYER_IDS` 已授权负责人指定的微信游戏账号。
- 负责人创建的 CloudBase 用户名：`swimming_admin`，UID：`2103882946932273154`。已于 2026-09-27 00:26:52 加入 `ADMIN_WEB_USER_IDS`，函数状态正常；密码仅由负责人持有。
- 负责人另行明确授权扫码账号 UID `2103899588243521537`。2026-09-27 01:35:11 保存后的 `ADMIN_WEB_USER_IDS` 已核对为 `2103882946932273154,2103899588243521537`，原账号权限保留。微搭实测 `me` 返回 `{"ok":true,"actor":"web:2103899588243521537"}`。
- 上述云函数名单与微搭应用访问权限、模型权限是不同设置，不能把名单配置完成当作管理应用已经可用。
- `players` 保存玩家档案；`operations` 保存游戏操作回执；`adminAudit` 保存管理修改前后记录；`counters` 保存发号进度。
- 四个集合保持普通客户端不可读写。管理修改应复用云函数的版本检查和事务审计；直接通过控制台改数据库不会自动执行这些业务逻辑。

## 日常使用：官方数据库控制台

[直接打开 players 玩家存档](https://tcb.cloud.tencent.com/dev?envId=cloud1-d5gn11660fac3375c#/db/doc/collection/flexdb/tnt-9cyjcvd8c/players)

这是腾讯云自己的管理页面，不是我们发布的网站，不依赖下文微搭测试域名。无需购买或配置域名，也不用运行本地程序；其他电脑登录有当前云环境管理权限的腾讯云账号后也可进入。沿用腾讯云控制台的扫码登录，不需要填写此前创建的 `swimming_admin` 应用账号。控制台权限与云函数的 `ADMIN_WEB_USER_IDS` 名单是两套不同权限。

2026-09-27 实际核查：

1. 左侧「文档型数据库」→「集合管理」→ `players`，已看到玩家“小虾2637”的档案。
2. 顶部有「筛选」「导入数据」「导出数据」入口。昵称不唯一，定位后仍需核对 `playerId`。
3. 点击文档右侧铅笔进入编辑模式，展开 `profile`，可以看到金币 `coins`、昵称 `nickName` 和角色数据 `characters`。
4. 点击 `coins` 这一行右侧铅笔，官方弹窗提供「字段 coins」「类型 number」「值」输入框及「确定／取消」。这里填写的是目标金币总数，不是增加数量。本次仅打开弹窗并取消，没有修改数值，退出编辑后仍为 0 金币、revision 1。
5. 角色等级字段路径为 `profile.characters.<角色ID>.level`，需展开对应角色；这不是把所有角色统一设置为一个等级。

**直接改数据库的区别：** 控制台保存字段不会自动调用 `swimming-admin`，也不会自动递增业务 `revision`、清理待结算赛事或生成 `adminAudit` 记录。它满足直接看表、改字段的需求，但不等同于现有补偿云函数的完整流程。修改前让玩家退出游戏并确认无待结算赛事，保留原值；正式补偿若需要版本检查和业务审计，可继续使用已保留的管理云函数。不要随意修改账号标识、schema 或清空回执集合。

## 玩家短 ID（2026-09-27 接入）

- 客户端：大厅昵称下显示 `ID：10000`；点击头像，面板显示完整编号与「复制 ID」。本地模拟档显示「本地测试，无正式 ID」，不会冒充云端编号。负责人已重新导出，本次新包的编号显示已实测。
- 玩家提供短 ID 后，在 `players` 的「筛选」中查询**顶层数值字段** `uid` 等于 `10000`，不是 `profile.uid`，也不是字符串 `"10000"`。找到后核对昵称和档案再编辑。
- `playerId` 与 `_id` 仍为内部的 64 位标识，用户无需记忆。云函数 `search` 的 `data.term` 支持短 ID 数字字符串、完整昵称和内部长 ID，返回 `uid` 与 `playerId`；后续管理写入仍使用返回的内部 `playerId`。
- `counters/playerUid.lastUid` 只记录最近分配号，首次设 9999。它与玩家记录在同一事务中更新，不能手动重置或随存档清理。
- 短 ID 只用于展示和查人，不是登录凭据。原管理员 CloudBase UID、微信身份校验与玩家公开 `uid` 是不同概念。
- 联机影响：不将此编号替换微信房间的 `clientId`，不修改房间协议、对局快照或比赛结果；只在云档载入/刷新事件中更新界面。

### 本次验证与部署边界

- 离线回归：云存档 21 项、弹窗 5 项、启动流程 12 项，共 38 项通过。发号测试使用可产生事务冲突的乐观并发数据库替身，覆盖重复账号、不同账号、失败回滚、旧档补号与编号稳定性；不冒充腾讯云真实并发压测。
- TypeScript 5.4.5 类型检查通过；本机无全局 npx，使用已有缓存的固定版本与仓库规定参数执行。`pnpm fonts:build` 与 `pnpm fonts:check` 通过；两套云函数部署包已重新生成。
- 腾讯云已创建 `counters`，选择 `ADMINONLY`；控制台读回 `_id: playerUid`、`lastUid: 9999`。这不是一张新玩家表，只是服务端发号计数。
- 两套云函数通过微信开发者工具「上传并部署：云端安装依赖」更新到现有环境，腾讯云详情均显示正常：`swimming-player` 修改时间 2026-09-27 02:51:27，`swimming-admin` 为 02:52:39。未改管理员名单，未提交金币、等级或回档操作。
- 负责人重新导出后，核对 `build/wechatgame/game.js` 与项目配置时间为 2026-09-27 02:58:09，产物包含短 ID 和复制按钮代码。在微信开发者工具登录，玩家“小虾2637”在大厅和头像面板均显示 `10000`；重新编译加载、再次登录后仍为 `10000`。
- 腾讯云官方控制台用 `uid`／数字／等于／`10000` 筛选，返回原玩家唯一记录，内部 `playerId` 为 `32e5e3340e050899c67ba888ce3329e0f630ade66d66823a202e8d816f4a1a2c`。`revision` 仍为 1、`updatedAt` 仍为 1790427700289；游戏显示金币 0、当前角色等级 1。本轮没有提交金币、等级或回档修改。
- 复制到系统剪贴板尚未验收：自动点击后未观察到「已复制」，粘贴仍是旧剪贴板内容；随后头像及其他普通按钮的自动点击也未响应，恢复模拟器缩放和重载后仍未恢复，暂不能区分自动化触控问题与游戏交互问题。不能将复制功能标为通过，需要后续实际点击并粘贴核实。
- 本轮属于微信开发者工具新包验证；iOS／Android 真机显示、触控与剪贴板尚未验证，腾讯云真实多账号并发发号也未实测。

## 之前创建的微搭应用（历史记录，非日常管理前提）

1. [云开发控制台](https://tcb.cloud.tencent.com/dev)，选择上述现有环境。
2. 「文档型数据库 → 数据模型 → 基于集合创建模型」。已从现有 `players` 生成标识为 `players`、名称为「玩家云存档」的数据模型，权限保持 `ADMINONLY`。模型管理表格实际读到原有 1 条档案，revision 为 1；未修改玩家记录。
3. 「微搭低代码 → 管理应用 → 从数据模型创建」。已创建「划水大师管理后台」，应用 ID `app-8ibl0h9c`，使用托管登录页。正式版 0.0.1 已部署，正式地址已实际登录并读到玩家详情。

### 已发布微搭应用地址（备用，不作为默认入口）

- [划水大师管理后台](https://cloud1-d5gn11660fac3375c-1496443607.tcloudbaseapp.com/app-8ibl0h9c/production/)
- [腾讯云云后台工作台](https://cloud1-d5gn11660fac3375c-1496443607.tcloudbaseapp.com/cloud-admin/)：已实际验证“自定义应用”区域显示「划水大师管理后台」，扫码账号可进入。
- 使用已授权的微信扫码账号登录；不需要启动任何本地程序。其他电脑可以打开上述地址，数据权限仍由登录账号决定。
- 输入完整昵称或玩家 ID 查询，点击「查看」读取金币和角色等级。选择调整内容，填写数值和原因，先点「预览修改」，核对后再「确认提交修改」。金币填写增减量，等级填写目标等级。
- 「查询修改记录」显示修改人、原因、前后差异和存档版本。网络结果未知时只重试原请求；不要另开同一笔加币操作。
- 当前默认域名是腾讯云测试域名，有访问提示和期限，控制台显示有效期至 2026-09-27 20:47:23。云后台工作台也使用同一域名，不能绕过此限制。此限制仅针对已发布应用的访问地址，不影响上文官方数据库控制台或游戏云存档。本次未购买套餐、域名或升级，不再为当前数据库管理需求推进域名配置。

### 当前验证结果

- 管理首页使用腾讯云原生 JSX 组件 `jsx1`，代码配置保存在微搭编辑器内；不运行本地服务，也没有引入第三方组件库。
- 原自动生成数据表格及其新建、删除、导出、导入和旧行操作已移除；旧编辑页的直写表单组件已移除。管理首页的查询与修改统一调用 `swimming-admin`，没有放宽模型或数据库权限。
- 扫码账号实际读到玩家“小虾2637”，金币为 0、revision 为 1，详情显示全部 11 个角色等级均为 1；审计查询返回暂无管理修改记录。
- 金币调整界面已验证预览：显示原值、目标值、原因、版本和请求编号，然后取消。未提交真实玩家修改。等级表单已接入同一云函数流程，尚未做真实写入验收。
- 写入前重新核对版本，冲突要求重新审阅；提交前把原请求存入当前标签页的 `sessionStorage`，未知结果保留原请求重试，避免重复加币。成功写入由云函数事务记录审计。
- 未授权的系统预览账号 `administrator` 曾实测返回 `FORBIDDEN / 无管理权限`。未给它授权，也未给普通玩家开放数据库。
- 发布检查要求使用“默认登录页”。负责人已分别明确授权默认登录页、跨应用共享登录态及发布。同步按钮一度显示通过，但设置仍指向应用专属登录页；最终在「应用设置 → 访问控制」直接选择“默认登录页”，保持托管登录、不开公开页面，保存后页面明确提示成功，发布检查通过。正式版 0.0.1 已实际打开，沿用扫码账号 `2103899588243521537` 成功读取列表及详情。登录配置提示最长 10 分钟生效。
- 编辑器已显示「发布成功」，最新发布版本 0.0.1。云后台入口、正式页面登录和玩家读取已验证。默认域名仅供开发测试且存在有效期，不能作为已完成的长期运营访问方案。

### 代理导致预览空白

2026-09-27 排查发现，本机 Clash 全局扩展脚本强制 Chrome 全部走代理，覆盖了订阅默认分流。加入腾讯云控制台、CloudBase 及相关静态资源域名的优先直连规则后，运行规则列表确认生效，原空白预览恢复并显示实际数据。保留既有代理规则；未关闭代理、重置网络或重启浏览器。个人代理配置不纳入项目版本管理。

当前环境是微信体验版。微搭页面显示发布应用额度为 3 个，具体功能是否需要升级仍以实际操作为准。此前添加自定义网页跨域来源时被套餐限制，本次没有购买或升级套餐，也不以此推断微搭管理应用不可用。

## 后续验证

官方数据库控制台的玩家读取和金币编辑弹窗已验证，未提交改档。微搭应用保留现状；其写入成功路径仍未在指定测试账号上验收，域名期限不再是本次需求的阻碍。不要对真实玩家发币或回档作为随意验证。

管理员修改经济或等级前，玩家应回到大厅。赛中不刷新角色属性，管理变更在重新读取云档后生效；游戏联机逻辑不受本次后台页面替换影响。
